{
  "name": "intmap-ops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IntMap — a Vite-built static site (#R175) plus the operational quality baseline around it (CI, static checks, browser smoke tests, internal-QA runner, release/rollback/monitoring tooling). `dependencies` are what the browser ships; `devDependencies` are build/CI only.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "//serve": "(#R175) `serve` still means 'give me the real site on 4173' as the standing docs say — it just builds first now. `preview` skips the build when dist/ is already current; `dev` is the HMR loop.",
    "serve": "npm run build && node scripts/serve.mjs --root dist",
    "preview": "node scripts/serve.mjs --root dist",
    "check:static": "node scripts/static-checks.mjs",
    "//check:engine": "(#R178) the renderer-decoupling gate: only js/geo-engine.js may name MapLibre. Parses, so a comment mentioning `map.` never trips it.",
    "check:engine": "node scripts/engine-coupling.mjs --gate",
    "//check:i18n": "(#R239) the ONE translation gate — every language against every surface it can be translated on (keyed table wherever it is declared, the inline L() table, the five positional arguments, the two reading pages, and the data-i18n keys the markup asks for). `--todo <code>` prints the commands that close a gap. Runs inside `npm test`.",
    "check:i18n": "node scripts/i18n-audit.mjs --gate",
    "//check:docs": "the cross-document gate: the facts written down in more than one place (what is served, how many Edge Functions there are, which languages exist, how production is deployed) are compared with the repository AND with each other. It also enforces that Architecture.md stays a current-state spec rather than a changelog.",
    "//check:companies": "(#R354) the company-atlas gate. data/companies/ is built from Wikidata, OpenStreetMap, SEC EDGAR and GLEIF, and the builder is written to DROP anything it cannot source — this proves it did. Twenty checks, numbered the same way in docs/COMPANIES.md §7: no money value without a currency and a period, no facility without a source and a stated position precision, no 0,0 coordinate, no duplicate id, and the nineteen companies the brief names must still resolve to their own identity. `--report` prints the coverage table.",
    "check:companies": "node scripts/companies-audit.mjs --gate",
    "check:docs": "node scripts/doc-facts.mjs --check",
    "//check:agents": "(#R503) the agent-context gate. The standing instructions live in AGENTS.md (both products read it), and the round procedure, the execution strategy and the five roles live under .agents/ — provider-neutral. scripts/agent-sync.mjs RENDERS those into .claude/agents, .claude/skills and .codex/agents, because each product only reads its own location; this re-renders and compares, so editing a copy fails instead of creating a second 正本. It also measures AGENTS.md against Codex project_doc_max_bytes (32,768): MEASURED #R503 that the overflow is dropped silently — the head of the file loads and the tail does not.",
    "check:agents": "node scripts/agent-sync.mjs",
    "check:archfiles": "node scripts/arch-files-check.mjs --check",
    "//build:countryfacts": "(#R453) restcountries.com was withdrawn — every /v3.1 and /v5 path 301s to a 261-byte deprecation notice whose redirect carries no Access-Control-Allow-Origin, so `enrichCountry()` failed on every country card and `catch(e){}` made that look exactly like a country with no neighbours. The six facts it supplied are built in now: mledoze/countries (REST Countries' own upstream, ODbL) for capital / currency / languages / land borders / UN membership / demonym, and the IANA time-zone database for standard-time offsets, keyed by the code js/countries-ui.js itself derives. NOT a gate and NOT in `npm test` — it needs the network; the committed file is checked offline by tests/r453-checks.test.mjs and the rows are checked ON THE SCREEN by tests/r424.spec.js. `--check` re-derives and compares.",
    "build:countryfacts": "node scripts/build-country-facts.mjs",
    "//build:coastline": "(#R495) 「海から200km以上」 had no measurement anywhere in the program. This builds data/coastline.json.gz from Natural Earth 1:10m physical `coastline`, simplified to a 2 km tolerance and split into the world OCEAN (`coords`) and the landlocked seas the same layer carries (`enclosed` — the Caspian, 12 parts, measured). NOT a gate and NOT in `npm test`: it downloads 60 MB of GeoJSON, and a CI runner has no cache for it — the committed artefact is checked OFFLINE by tests/r495-checks.test.mjs, which also re-derives every distance with a second, different spherical formula. `--check` re-derives and compares.",
    "build:coastline": "node --max-old-space-size=4096 scripts/build-coastline.mjs",
    "//build:rail": "(#R388) OpenStreetMap の全世界鉄道。掃引はセル単位でキャッシュするので再開可能——2回目以降のビルドはネットワークに触らない。",
    "build:rail": "node scripts/rail/fetch.mjs && node --max-old-space-size=8192 scripts/rail/build.mjs && node scripts/rail/stations.mjs",
    "//check:catalog": "(#R278) the Atlas catalogue gate: every capability the dispatch in js/atlas-console.js can execute must be described to the planner in function SYS(). An action the catalogue does not describe does not exist for the planner (#R115) — six of them, the road-network isochrone included, were invisible until this gate was written. Runs inside `npm test`.",
    "//check:wars": "(#R381) scripts/build-wars.mjs shipped in #R349 with a --check mode and NOTHING THAT RAN IT, so the committed data/wars.json could drift from the record in scripts/wars/ and no gate would notice. It re-derives the file and compares it byte for byte, and it runs the whole self-audit on the way: the gazetteer cross-check, the CShapes date check, the cut check, the named-city control checks (#R409 added the sixteen spans where only `control` describes a country a front otherwise divides — the count moves whenever the record does, so it is not written here) and the unused-anchor check.",
    "check:wars": "node scripts/build-wars.mjs --check",
    "//check:histcities": "(#R427) the historical CITY-name record. data/hist-cities.json is re-derived from scripts/histcities/ and compared byte for byte, and the whole self-audit runs on the way: every tile key resolved against data/gazetteer-world.json.gz for position and country, and — the one that matters — every key checked for naming a SECOND populated place anywhere on Earth. The label is rewritten by matching the tile’s own name, so a shared spelling would rename the other city too; that fails the build unless the row declares the exception with a trailing «!» and a written reason.",
    "check:histcities": "node scripts/build-hist-cities.mjs --check",
    "check:histborders": "node scripts/build-hist-borders.mjs --check",
    "check:catalog": "node scripts/atlas-catalog.mjs --check",
    "//check:perf": "(#R311) THE STARTUP BUDGET, and before it nothing in CI weighed a single byte of the deploy — `vite build` printed «Some chunks are larger than 3000 kB» on every run and exited 0. It reads .perf/build-report.json (written by the build itself, scripts/build-report.mjs) and judges the two halves separately: the EAGER set is ratcheted in BOTH directions (over the ceiling is a regression; well under it means the ceiling stopped asserting anything and must be lowered with --update, the same rule #R194 gave the test-time budget), while ASYNC chunks and the dist/ totals get a ceiling only, because a 4.8 MB Cesium chunk a MapLibre session never fetches is not a startup cost. Needs a build first, so it runs as its own CI step after `npm run build` rather than inside `npm test` (which already builds once, in the browser half, concurrently).",
    "check:perf": "node scripts/perf-budget.mjs",
    "//check:assets": "(#R322) THE OTHER HALF OF THE DEPLOY. check:perf weighs what Rollup produced; this weighs what was COPIED — data/ is 55.8 MB of a 105.7 MB tree and none of it passes through the bundler. Every file in dist/ is matched against the strings the source actually contains (js/ src/ css/ *.html sw.js, plus the small manifests under data/ that name assets the JavaScript never spells out), and a file nothing mentions, a payload shipped twice, or a file over the per-file ceiling with no recorded reason fails the gate. Needs a build first, so it runs beside check:perf rather than inside npm test.",
    "check:assets": "node scripts/asset-report.mjs --check",
    "//perf:report": "(#R311) the same measurement without the judging — eager vs async, raw/gzip/brotli, the largest eager modules, duplicated modules. `npm run build` writes it; this prints it.",
    "perf:report": "node scripts/build-report.mjs",
    "check:capabilities": "node scripts/atlas-capability-audit.mjs --check",
    "audit:capabilities": "node scripts/atlas-capability-audit.mjs --json",
    "//master": "(#R282) the master copy is the OneDrive working directory (AGENTS.md §6), and until this existed no step in the workflow was responsible for it: a round could merge, deploy and back up to USB while OneDrive stayed fifteen commits behind. `master:sync` is the last step of every task; `master:check` is the gate the USB mirror runs first. NOT in `npm test` — on a CI runner the checkout is a detached PR ref and 'behind origin/main' is the correct state there. What CI can prove about the rule is in tests/r282-checks.test.mjs.",
    "master:check": "node scripts/master-sync.mjs --check",
    "master:sync": "node scripts/master-sync.mjs --sync",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:qa": "playwright test tests/internal-qa.spec.js",
    "test:e2e": "playwright test",
    "//test:profile": "(#R184) the renderer PROFILING instrument — frame time beside a sharpness measure, swept over the anti-aliasing and LOD settings. Excluded from the default run (see playwright.config.js): it is a measurement, not a gate.",
    "//test:profile2": "(#R191) …and it STILL did not run. On Windows spawnSync('npx.cmd') without a shell cannot resolve the launcher, so it returned status null, `||0` turned that into a clean exit, and the command printed nothing at all and reported success — the same silence #R190 set out to fix, one layer down. `shell` on win32 makes the launcher resolvable, and a null status is now a FAILURE rather than a pass. Verified by running it: 1 passed (2.8m), with the sweep table on stdout.",
    "test:profile": "node -e \"process.env.IM_PROFILE='1';const{spawnSync}=require('child_process');const r=spawnSync(process.platform==='win32'?'npx.cmd':'npx',['playwright','test','--workers=1'],{stdio:'inherit',env:process.env,shell:process.platform==='win32'});if(r.error)throw r.error;process.exit(r.status==null?1:r.status)\"",
    "test:security": "node --test tests/security-logic.test.mjs",
    "test:monitor": "node --test tests/monitor-logic.test.mjs",
    "//test:checks": "(#R529) DISCOVERED, NOT LISTED. This used to be one hand-maintained literal naming all 292 files by hand, and a file left out of it never ran — so it was not a weaker test, it was not a test. Measured (#R301): tests/r210 and tests/r211 had never once been executed, and when they finally were, five of r211's twelve assertions were red, the earliest broken ninety rounds before anybody saw it. Three guards were then stacked on the literal rather than on the hazard: the disk comparison (#R301), the against-itself rule after the list named one file twice for twenty-two green rounds (#R385), and a SOURCE rule for the one file whose name predated the convention (#R390). #R529 removed the literal instead — `node --test` finds every `tests/**/*.test.mjs` itself, tests/security-logic.mjs was renamed so the naming convention has no exception left, and scripts/check-test-list.mjs is gone with it. The one question that survives — a .mjs under tests/ that imports `node:test` but is NOT named *.test.mjs is invisible to the runner — is asked by `check:static` against the disk. ⚠ the quoted glob is a Node ≥21 argument form (Node 20 searched DIRECTORIES and did not glob); `engines` and .nvmrc say what this runs on.",
    "test:checks": "node --test \"tests/**/*.test.mjs\"",
    "//test": "(#R205) the source-level half (static checks, the engine gate, the budget, the node test files `test:checks` discovers) and the browser half share nothing, so they run AT THE SAME TIME through scripts/test-parallel.mjs — npm test costs max(a,b) instead of a+b. (#R196) the browser half runs through scripts/run-tests.mjs, which orders the specs by their MEASURED time (longest first) and gives the specs that need a machine to themselves one worker — the same plan CI has used since #R195, applied to the local run. Same files, same config; it falls back to a plain `playwright test` if the plan cannot cover every spec.",
    "test": "node scripts/test-parallel.mjs",
    "//test:seq": "(#R205) the same steps in the old sequential order, for when an interleaved log is in the way of reading a failure.",
    "test:seq": "node scripts/static-checks.mjs && node scripts/engine-coupling.mjs --gate && node scripts/atlas-catalog.mjs --check && node scripts/companies-audit.mjs --gate && node scripts/build-wars.mjs --check && node scripts/build-hist-cities.mjs --check && node scripts/test-budget.mjs && npm run test:checks && node scripts/run-tests.mjs && node scripts/perf-budget.mjs",
    "//test:deep": "(#R203) the half of the suite that does NOT stand in front of a push. (#R372) MEASURED: the deep tier is 96 spec files against the core tier's 6 — and the number written here said 59, docs/TESTING.md said 64 and scripts/worktree.mjs said 27, all at the same time; (#R500) three of the four copies had drifted apart AGAIN (86 here / 92 in docs/TESTING.md / 82 in scripts/worktree.mjs) and `deep-tier-size` in scripts/doc-facts.mjs now measures every one of them; it is derived, so re-measure with `node -e \"import('./scripts/tiers.mjs').then(t=>console.log(t.tierSpecs('deep').length))\"` rather than trusting this sentence, because #R204/#R207 turned the split from a list into a PRICE (scripts/tiers.mjs, CORE_MAX_S) — a spec stands in the gate only if it costs at most one second, so nearly every per-round file is deep. Locally 89 of them run at two workers and the five `solo` Cesium files then run at one; scripts/test-budget.mjs measures the whole suite at __MIN__ min of serial browser time. It runs NIGHTLY (ci.yml `schedule`) and on demand, NOT on push (#R207). Run it before opening a PR that touches 3-D, Cesium, the simulators or the physics. `test:all` is both. ⚠ scripts/tiers.mjs derives the split; tests/durations.json supplies the measured seconds and the `solo` list, not a `deep` list. ⚠ When it goes red nobody is watching by default — scripts/deep-alarm.mjs opens an issue and `node scripts/worktree.mjs status` prints last night's verdict (#R304).",
    "test:deep": "node -e \"process.env.IM_TIER='deep';import('./scripts/run-tests.mjs')\"",
    "test:all": "node -e \"process.env.IM_TIER='all';import('./scripts/run-tests.mjs')\"",
    "report": "playwright show-report",
    "check:testbudget": "node scripts/test-budget.mjs",
    "//check:testbudget": "(#R197) the suite has a CEILING and it only ever goes down — see scripts/test-budget.mjs. A round that adds test time must take at least as much out somewhere else; that is what stops the per-round spec files accumulating into 「何重にも」.",
    "//prebuild": "(#R232) js/locales/_langs.js is the language list for the two static reading pages (no bundler there, so no import.meta.glob). Regenerating it here is what makes 「adding a language = adding one locale file」 true for those pages too; tests/r232-checks fails if the committed copy is stale.",
    "prebuild": "node scripts/i18n-langs.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "acorn": "^8.17.0",
    "acorn-walk": "^8.3.5",
    "js-yaml": "4.3.1",
    "opencc-js": "^1.4.1",
    "opentype.js": "^2.0.0",
    "vite": "6.4.3"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^2.0.3",
    "@supabase/supabase-js": "2.58.0",
    "@turf/along": "6.5.0",
    "@turf/area": "6.5.0",
    "@turf/bbox": "6.5.0",
    "@turf/bbox-clip": "6.5.0",
    "@turf/bearing": "6.5.0",
    "@turf/boolean-point-in-polygon": "6.5.0",
    "@turf/buffer": "6.5.0",
    "@turf/center": "6.5.0",
    "@turf/centroid": "6.5.0",
    "@turf/circle": "6.5.0",
    "@turf/convex": "6.5.0",
    "@turf/distance": "6.5.0",
    "@turf/great-circle": "6.5.0",
    "@turf/helpers": "6.5.0",
    "@turf/kinks": "6.5.0",
    "@turf/length": "6.5.0",
    "@turf/point-on-feature": "6.5.0",
    "@turf/turf": "6.5.0",
    "@turf/union": "6.5.0",
    "cesium": "^1.143.0",
    "html2canvas": "1.4.1",
    "katex": "0.16.21",
    "maplibre-contour": "0.1.0",
    "maplibre-gl": "5.24.0",
    "pbf": "^4.0.1",
    "polygon-clipping": "0.15.7",
    "satellite.js": "^7.1.0",
    "topojson-client": "3.1.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "overrides": {
    "dompurify": "3.4.13",
    "nanoid": "3.3.18"
  }
}
