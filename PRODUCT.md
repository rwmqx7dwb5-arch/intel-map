# IntMap — 製品 (Product)

> **IntMap は何のためにあり、誰のためのもので、何をやらないか。**
>
> - **正本**: このファイル。「何ができるか」（機能一覧）と「何を目指すか」（Atlas の到達点）は
>   同じ問いの両面なので、1つのファイルに置いてある。
> - **読む人**: 機能を足す／削る／並べ替える判断をする前の全員。
> - **更新条件**: ユーザーが**製品としての方針**を変えたとき、および**機能を追加・撤去したとき**。
> - **書いてよいこと**: 目的・対象・優先順位・非目標・機能の一覧。
>   **書かないこと**: 実装（→ [`Architecture.md`](Architecture.md)）、
>   判断の理由（→ [`DECISIONS.md`](DECISIONS.md)）、履歴（→ `DEV-NOTES.md`）。
>
> ⚠ **このファイルの §1・§2 は、ユーザーが述べた指示・`CONSTITUTION.md`・実装済みの挙動から
> 起こしたものである。** 推測で製品方針を足さない。方針が変わったら、変えた本人の言葉で書き換える。

---

## 1. 何であるか

IntMap は、**世界のニュース・気候・人口・経済・地政学データを一枚の地図に重ねて理解するための
Web アプリ**である。アカウント無しで開いてすぐ使え、ログインすると AI 機能と設定同期が付く。

- **対象**: 「いま世界で何が起きていて、それはどこで、周りに何があるのか」を自分で確かめたい人。
  専門家向けの GIS ではなく、**地図の上で問いを立てて答えを出す**ための道具である。
- **配信**: 公開 Web サイト（GitHub Pages）。インストール不要。PWA としても入る。
- **費用**: 無料。維持費は寄付（Stripe）で賄う。AI は**アカウント単位の1日上限**で配る。
- **9言語**（en / ja / de / ru / es / zh-Hant / zh-Hans / fr / ko）で、**画面に出る文字は全部**が対象。

## 2. 優先順位と非目標

### 2.1 守ること（`CONSTITUTION.md` が正本）

1. **既存機能の削除・縮小は、確認を取ってから行う。勝手にはしない。**
   重複・死んだ機構・役目を終えた画面は、必要と判断すれば**提案する**（正本は
   `CONSTITUTION.md` §0 の 3）。承認されるまでは削らず、移動・追加・無効化で対応する。
   ⚠ **Atlas だけは別**——実装は削ってよいが、**到達可能な能力と回答品質は削らない**
   （`CONSTITUTION.md` §5）。
2. **報告された不具合は実在する現象**として扱い、根本原因で直す。
3. **表示するものは正直であること。** 取得できなかったものを「無かった」と描かない。
   単位・出典・時刻・欠損を画面に出す。**空白を「平穏」に見せない。**
4. **API キーをフロントに置かない。** AI はサーバー側（Supabase Edge Function）で実行する。

### 2.2 いま優先すること

1. **Atlas から全機能へ到達できること**（§4 の第1段）。新機能は UI と Atlas の両方から届く。
2. **地図の上で答えが完結すること** — 文章だけで答えない。答えは地図の状態として現れる。
3. **モバイルで実用に耐えること**（iOS 風・1タップ＝1トグル・地図が見えたまま）。
4. **9言語すべてで同じ画面**であること。

### 2.3 やらないこと（現時点）

- **保存・外部出力・PDF・プレゼン生成・高度な監視**は後回し（§4 の 20 項目でユーザーが明示）。
- **地域監視 (Area Monitors)** は入口ごと撤去中。基盤は残してあるが、利用者からは到達できない
  （`Architecture.md` §18）。
- **有料プラン**は用意していない。`plan` 列と `monitor_limit()` は接続点として存在するだけ。

---

## 3. 主要機能一覧 (Features)

> ここは**何ができるか**の一覧。内部構造は [`Architecture.md`](Architecture.md) §7〜§10 と
> [`docs/MAP-LAYERS.md`](docs/MAP-LAYERS.md) へ。

### 3.1 地図とニュース

- **出来事単位のニュース** — News は**記事1本ではなく出来事1件**を項目にする。同じ出来事を報じた
  複数の記事は1つの Event にまとまり、地図には **1 出来事 = 1 ピン**が立つ。
  **1 つの出来事は IntMap の中だけで理解できる** — カードは要点の1文を出典付きで出し、開くと
  **何が起きたか**（各媒体が実際に公表した文をそのまま・1行ごとに媒体名）／**主要な数字**／
  **最新の記事で更新された点**／**媒体間で一致している点と食い違っている点**／どの媒体がいつ何と
  書いたか・初報はどこか・どれが同一資本系列の転載か、が並ぶ。外部記事へのリンクは
  「発信元で読む」＝**媒体ごとの二次導線**。
  ⚠ **書き換えも創作もしない。** 独立2媒体以上が本文を持つ出来事では、それらを1つの説明に
  まとめた段落が付くが、**1文ごとに根拠の断片を原文と機械照合してからでないと保存されない**
  （照合できなかった文は捨てられ、表示されない）。**上流が本文を配っていない出来事では、
  その旨と理由を書く**——「要約がありません」で読み込み失敗に見せない。
  IntMap はどちらの媒体が正しいかを言わない。書かれている数量を並べるだけ。
  8カテゴリ（国際／政治・紛争／経済／テクノロジー／科学・医療／気候・気象／災害／社会）で、
  一覧と地図を同時に絞り込める。★は出来事に付く。
  束ね方はサーバー側の決定論規則（時刻・場所・珍しい語の共有）で、AI の判断ではない。
  ⚠ **News の表示言語は英語**（見出しも要約も。UI の囲みは9言語のまま）。
  詳しくは [`docs/NEWS-EVENTS.md`](docs/NEWS-EVENTS.md)。
- **ライブニュースマップ** — 世界の見出しを「出来事が起きた場所」にピン表示。**1 ピン＝1 つの出来事**で、
  ピンを押すとその出来事の詳細（何が起きたか・媒体ごとの記述・数字・発信元リンク）が
  サイドバーに開く。72時間以内のニュースのみ表示。既定の出来事経路では、**どこの出来事かをサーバー側で AI が判定**し
  （決定論エンジンがフォールバック）、都市・港・国境検問所のように**国より細かい場所**まで降りる。
  **検索・過去の日付・多言語モードでは記事単位**に戻り、そこでの地点解析は**ブラウザ内の決定論エンジン**
  （AI もサーバー保存も使わない。`Architecture.md` §4）。
- **Globe / Flat / Satellite / 3D地形** 切替、コンパス、距離・面積計測、半径円 (Radius)、
  可視判定（見通し線・レーダー影）。
- **Chronos（統一時間）** — マスタークロック `window.IntMapTime` を動かすと、国境・国家・統計・レイヤーの年・
  予報時刻・ライブ衛星の位置がそろって動く（`Architecture.md` §7.4）。どの時計で読み書きするか
  （端末／UTC／地図中心の標準時／主要タイムゾーン）をパネルのプルダウンで選べる。
  **さかのぼれるのは 1850 年まで。** 各出典が届く範囲までそれぞれ動く（GDP・人口は 1850 年から／
  気候区は最古が 1901-1930）。
  **国境は下限の 1850 年まで「年」ではなく「日」で変わる。** 1886–2019 は CShapes の発効日そのままで、
  ドイツ再統一なら 1990-10-02 と 10-04 で世界が違う（変化日は 369 日ある）。
  **1850–1885 は OpenHistoricalMap**（`data/hist-borders.js`・記録 494 件・窓の中の変化日 216 件）。
  それまでこの 36 年は 1880 年の1フレームを共有していたので、1850 年にも 1880 年の国境が出ていた。
  いまは両シチリア王国も南北戦争の南部連合もハノーファー王国も、その日そこにあったとおりに出る。
  Chronos の**国境ステッパー**（`‹ ›`）が実際の変化日を1つずつ歩き、いま画面に出ている世界が
  生まれた日を表示する——176年ぶんのスライダーでは 1920-10-28 に指で止められないので、
  密な年代に届く操作はこれになる。
  消えた国は後継国に分解せず、その時代の国家として1行で出る（オーストリア帝国 → オーストリア＝
  ハンガリー、朝鮮 → 大韓帝国 → 大日本帝国、東インド会社 → イギリス領インド帝国…）。
- **都市名ラベルも、その年の名前になる** — **611都市・688の歴史名**が9言語すべてで入っている。
  ⚠ 名前が同じでも**別の都市は別の都市**として扱う——高知市は「Kochi」でもコーチンにはならないし、
  カルーガ州のキーロフはヴャトカにならない。どの都市かは綴りではなく**綴りと位置**で決まる。
  1942年に行けばヴォルゴグラードは**スターリングラード**、1867年の東京は**江戸**、1960年の
  サンクトペテルブルクは**レニングラード**、1945年のカリーニングラードは**ケーニヒスベルク**、
  1965年のキンシャサは**レオポルドヴィル**、1981年のハラレは**ソールズベリー**、1929年の
  イスタンブールは**コンスタンティノープル**、1990年代のムンバイは**ボンベイ**。
  ソ連・ウクライナの脱共産主義化・ドイツ語圏／ポーランド・バルト・中央アジア・旧植民地の独立・
  日本統治下の朝鮮と台湾・カナダ北極圏のイヌクティトゥット語復帰まで含む。
  ⚠ **ラベルの位置は動かない**——地図タイル自身のラベルの文字だけを差し替えるので、
  現代の地図とまったく同じ場所・同じ重なり順で出る。記録に無い地名は1文字も変わらない。
- **両大戦の日ごとの勢力変遷** — **第一次世界大戦と第二次世界大戦で別々のレイヤー行**（どちらも
  既定 OFF）。ON にすると**その日の支配地域・戦線・進行中の作戦**が出る。全戦域（西部・東部・
  イタリア・バルカン・中東・北アフリカ・大西洋・太平洋・中国・ビルマ）。国や戦闘をクリックすると
  当時の名前・日付・**種別**・**投入兵力と死傷（分かっているもの）**・Wikipedia。
  - **凡例の中に、その大戦の日を動かすスライダーと再生ボタン**がある（1日／5日／15日刻み、
    日付の直接入力つき）。⚠ **これはタイムマシン (Chronos) を動かさない**——大戦の日を送るために
    アプリ全体の時刻を動かすことはしない。逆に、タイムマシンを動かせばこのレイヤーはついてくる。
  - 作戦は**種別ごとに色分け**（陸戦・海戦・航空作戦・包囲戦・上陸／空挺・宣戦や条約・首脳会談・
    大量虐殺や収容所・蜂起）され、**規模の大きいものほど大きな点**で描かれる。
  - ⚠ **戦線は記録が位置を伝えている日付にだけ引き、次の記録まで保持する**（線の日付を凡例に出す）。
    滑らかに動かすための補間はしない。
- **海岸線・湖岸線**（基本表示）— 国境線と全く同じ線で、海と湖の岸を描く。**既定でオン**
  （色面の下で「どこを見ているか」が常に分かるように）。消せば消えたままで、風レイヤーか気温レイヤーを
  点けたときに1回だけ提案が戻る。
- **国情報** — Countries を ON にして国をタップ → 統計カード＋詳細ポップアップ（世界銀行の時系列、比較）。
- **Isolate（この国だけ表示）** — 1国だけ残して周囲をマスク。タイムマシンで遡った歴史的国家も、
  その当時の版図のまま Isolate できる。
- **共有 (Share this view)** — 位置・ズーム・投影・ベース地図・選択中の全レイヤー・時刻・比較状態を
  **URL 自体**に格納する。リンクを開くと状態を完全復元する。
- **スクリーンショット**、**お気に入りレイヤー**、**レイヤープリセット**、**PWA (Service Worker)**。

### 3.2 データレイヤー

- **100本以上のデータレイヤー**。`js/data-layers.js` の `GROUPS` が **18 の棚に 140 行**を配り、
  `GROUPS` に無い行は末尾の「Others」と「ベータ」へ落ちる（棚の一覧と方針は `docs/MAP-LAYERS.md` §7.2）。
  棚を開くと、**その分野で名指しされた行だけ**が並ぶ（実測 56 行）。残りは棚の中の
  **「その他N件」**の後ろに畳まれていて、1回押せば出る（実測 84 行）。
- **「基本表示」はデフォルト／クリーン／カスタムの3択**。デフォルトは出荷時の地図、クリーンは
  地名も国境も道路も無い素の地図、カスタムは11個のスイッチを1つずつ選ぶ。
  Atlas や他の機能が基本表示を動かしたら、**表示は自動的に「カスタム」へ切り替わる**——
  「デフォルト」と書かれた見出しの下がデフォルトでない、という状態を作らないため。
- 分野は、気候・気象／宇宙／海洋／地形・標高／自然・土地被覆／人口／災害・緊急／政治・統治／
  軍事・安全保障／医療・衛生／IT・技術インフラ／経済・貿易／社会・教育／交通・輸送／農業・食料／
  エネルギー・資源。
- **GIBS ラスタの凡例は実際の NASA カラーマップ XML から生成**するので、凡例の色が地図と一致する。
- **世界の鉄道は、線路1本ずつの実タグで描く**（OpenStreetMap・ODbL 1.0。仕様は `Architecture.md` §7.3c）。
  軌間・電化方式と電圧・最高速度・複線/単線・旅客/貨物・幹線/支線・高速鉄道・運行状態（運行中/建設中）・
  路線名と路線番号・運行会社・開業年を持ち、**塗り分ける軸を凡例の上で切り替えられる**。線をクリックすると
  その区間の全項目と OSM のオブジェクトへのリンクが出る。z6.5 から詳細セル、z8 から駅（事業者・網・UIC）。
  ⚠ **どの軸にも「OSM に記載なし」の区分があり、その灰色は回答のどの色とも一致しない。**
  タグの付与率は地域差が大きい（実測: 最高速度はイベリア 60%／インド 6%）ので、**灰色は「記載がない」
  以外の意味を持たない**——通っている国の主流値で埋めることはしない。
- **言語分布は、国ごとの最多話者言語を Glottolog の言語同一性の上で描く**（CIA World Factbook・パブリック
  ドメイン／Glottolog・CC BY 4.0）。204 か国について、出典が挙げるすべての言語を**挙げられた順に**、
  公表されていれば割合つきで、出典が与える**位置づけ**（公用語・共同公用語・地域公用語・国語・共通語・
  実務言語・少数言語）つきで持つ。国をタップすると内訳の棒グラフ、各言語の位置づけ、出典が名指して
  いない割合、そして主要言語の**系統経路**（Indo-European › Balto-Slavic › Slavic › … › Serbian）が出る。
  ⚠ **出典が割合を公表していない 107 か国は、専用の色で「割合の公表なし」と表示する**——最初に列挙された
  言語（多くは公用語）を最多話者言語として塗ることはしない。
- **世界銀行系の塗り分けは年を選べる**（凡例の年セレクタが `window.IntMapTime` を読み書きする）。
- **気象・災害警報は、その機関が発令した単位で塗る**（自前 13 フィード・47 か国 ＋ **WMO の CAP 登録簿**で
  さらに約 90 か国。`docs/MAP-LAYERS.md` §7.1）。
  **「発令なし」も区分単位で塗る**ので、静かな区分は隣の警報区域と同じ粒度で読める
  （世界の第1級行政区画を同梱している——247 か国・4,515 区分）。
  ⚠ **「発令なし」の灰色は、発令中の区域と同じ 1 枚の面**である——同じ色・同じ不透明度スライダーで、
  どの国も同じ扱い。**警報が置かれた区分に灰色は出ない**ので、色の下から灰色が透けることもない。
  ⚠ **灰色の形は「区分 − そこに重なる発表すべて」**である。機関の発表単位とこの地図の区分は同じ
  分割ではない（カナダの予報区は州にまたがる）ので、**またがる発表は切り取り**、区分より小さい発表は
  **穴**にする。区分を丸ごと捨てるのは、発表が区分を覆い尽くすときだけ。
  ⚠ **1つの市町村が複数の発表単位に分かれている場合は、その単位の形で塗る**（五條市北部／南部など、
  日本の 40 市町村＝89 単位）。片方に注意報が出ていても、もう片方は灰色のままになる。
  ⚠ **灰色は「読んだ。何も出ていない」という主張なので、それが真である範囲にだけ出す。**
  区分ごとに描けないほど遠いズームでは、**何かが発令されている国には国全体の灰色を出さない**
  （その国について「何も出ていない」は偽だから）。そこに見えるのは警報の形だけで、
  「どこで無いか」はズームを寄せると区分の灰色が答える。
  ⚠ **塗る範囲は、その機関が名指した範囲だけ。** 機関がより細かい単位について「発表なし」と言っている
  とき、その上の広い区域の発表でそこを塗り替えることはしない（日本の一次細分区域→市町村）。
  解除・終了した発表は塗らない（カナダ）。コードから親の行政区画を推測して県や省まで広げることも
  しない（中国）。**置けなかった発表は「置けなかった」と数えて印字する**——「発表なし」にはしない。
  **日本は、画面に入っている都道府県の境界をより細かい版へ切り替える**ので、寄るほど市町村の形が
  実物に近づく。
  **この地図が何も述べていない場所**——フィードの無い国と、まだ読めていない国——は**灰色斜線**で、
  空白を「平穏」に見せない。⚠ **斜線は線だけ**で、下に灰色を敷かない——2 つの見え方が同じ場所に
  重なると、区別しろと言われている 2 つの状態が区別できなくなる。**担当する国は書き足さず学習する**（機関自身のポリゴンが落ちた国は
  その機関の担当）。
- **気象レイヤーは自分の時刻を持つ** — 凡例の中の `<select>` に、モデルが公開している有効時刻だけが
  並ぶ（データの無い時刻は選べない）。スライダーはその**添字**の上を刻むので、指で掃いても
  データの無い時刻には止まらない。時刻を変えても地図は空にならず、**新しい絵が実際に出てから**
  古い絵が外れる。アプリ共通の時計（Chronos）とは結線していないので、天気の時刻を動かしても
  ニュース・国境・昼夜境界は動かない。
- **日付で選ぶラスタ（NASA GIBS 系）も同じ規則** — 製品ごとに**提供開始日・提供の遅れ・提供の間隔**を
  持ち、日付欄はその範囲でしか選べず、範囲外を渡されたら**その製品が実際に出している最も近い日**へ
  吸着して、**実際に描いている日**を凡例に書く。前へ／次へは 1 コマ分だけ動く。
- **気温は 1 つのレイヤー**で、予報モデルの数値だけを答える。
- **各気象レイヤーは読むモデルを選べる**（ECMWF IFS HRES 約9km/6日・NOAA GFS 約13km/16日・
  DWD ICON 約13km/5日）。**選択はレイヤーごと**なので「降水は GFS、気圧は ECMWF」が並べられる。
  凡例はモデル名・初期時刻・解像度・有効時刻を常に表示し、**画面に出ているモデル**を書く
  ——切替中は前のモデルを表示したまま「切替中」と添える。
  ⚠ **そのモデルが公開していない変数は選べない**（GFS 0.13° に海面気圧・CAPE・露点は無い）。
  選択肢は理由付きで無効になり、空の地図にはならない。
- **風の筋は風レイヤーだけのものではない** — 気温・最大瞬間風速・海面気圧・降水量（予報）の各凡例に
  「風のパーティクル」の箱があり、その場の色ラスタの上に**風の動き**を重ねられる（風レイヤー自体を
  点ける必要は無い）。**最大瞬間風速・海面気圧・降水量（予報）は最初からオン**——低気圧の渦も前線の
  シアも、筋があって初めて形として読めるからで、値として読む気温だけは最初はオフ。
  箱は**レイヤーごとに独立して記憶**され、一度触れば以後はその答えが優先される。
- **ライブ航空機は全球で、ズームの下限が無い** — 機体は `world`（フィードが知っている全機）と
  `view`（いま見ている範囲）の2つの経路で同じ在庫に流れ込む。**ズームが決めるのは大きさと細かさだけ**で、
  「そこにいるかどうか」ではない。数万機を **1 描画呼び出し**で描くので、3-D 表示のときに 4,000 機で
  打ち切って残りを地図の中心からの距離で捨てる、という上限も無い。
- **ライブ船舶はキー無しで出る** — 船舶レイヤーは、以前は利用者ひとりずつが aisstream.io のキーを
  取って設定に貼るまで「APIキーが必要です」と言って何も描かなかった。いまはサーバー側の 1 本の鍵と
  鍵不要の Digitraffic（バルト海・CC BY 4.0）を `ais-feed` が代表して読み、**見ている範囲の船**を
  全利用者に配る。自分の鍵を持つ人は今までどおりブラウザから直接ライブ配信を受けられる（任意）。
  仕組みは [`docs/SHIPS-ARCHITECTURE.md`](docs/SHIPS-ARCHITECTURE.md)。
- **飛行機のマークは「上から見た絵」ではなく、そのカメラから見た絵である** — 機首は
  **世界の中での実際の針路**を向き、地図を傾ければ**その下の地面と同じだけ**潰れて見える。
  真上から見ているときだけプランフォームの全形になる。
  ⚠ **細くなりすぎて読めなくなる手前で止まる**：飛行機ではなく点を描き始める大きさ（5 画素）が
  そのまま潰しの下限で、そこに達したマークは傾きに追従するのをやめる。
- **上流を読むのはサーバー1本だけ**（Edge Function `aviation-feed`。`Architecture.md` §6.2） —
  ブラウザは provider を直接叩かない。**TTL ごとに1回**の読み取りを全利用者で分け合うので、
  上流にかかる負荷は**利用者数ではなく時間に比例する**。⚠ **合成した機体は1機も描かない**——
  取得に失敗したときは最後の実データを保ったまま、そう言う。
  ⚠ **視野の問い合わせは「いちばん長く訊いていない空」から4枚ずつ買う** — 上流の予算は
  変えられないので、**同じ4枚を買い直すのをやめた**。広い視野は同じ予算のまま、
  ポーリングのたびに外へ向かって空を広げていく。

- **火山はカタログではなく、1 座ずつの記録である** — GVP 火山番号を結合鍵にして、
  同梱の 11,043 件の噴火履歴（VEI・日付・証拠方法）、火山型・地形区分・構造区分・主要岩石、
  地質の概要と写真、半径 5/10/30/100 km の人口がカードで開く。そのうえに**現在の状況**が
  **4 段のはしご**で載る——① USGS（米国：航空カラーコードと警戒レベル・VONA）、
  ② 気象庁（日本：噴火警戒レベル 1–5）、③ Smithsonian/USGS 週間報告（全世界の活動状況）、
  ⓪ **何も公表されていない**。⚠ **① ② ③ は別の機関の別の計器なので 1 つの数に混ぜない**——
  カードは機関自身の語彙で機関名を付けて述べ、⓪ は**静穏として描かず、言葉で述べる**。
  地図の色は**4 つの問い**（最終噴火／爆発規模／現在／周辺人口）から選べ、
  点の大きさはどのモードでも**記録上の最大 VEI** を表す。
  ⚠ **同梱カタログは GVP 完新世の全件に加えて、観測機関が現在の警戒レベルを公表している火山を含む**
  ——イエローストーン・ロングバレー・コソ火山地域・イサノツキ山は最後の噴火が完新世より前なので
  完新世の一覧には無いが、USGS が毎日レベルを公表している。**レベルが公表されている火山は地図に出る。**
- **火山に付く 3 つのレイヤー** — **火山灰域**は VAAC の advisory を受けて FIR が出した
  **国際 SIGMET のポリゴンと高度帯そのもの**（火山のまわりの円ではない。有効期限は時計が落とす）、
  **ハザード域**は**機械可読な GIS が公表されている火山だけ**（USGS のカリフォルニア 7 火山地域）で、
  **それ以外については「公表されていない」と述べ、推定の到達円は 1 つも描かない**、
  **SO₂** は噴煙が現れる高度帯の衛星全量（NASA GIBS / OMPS）。
  ⚠ **「噴火様式」はラベルを出さない**——GVP のデータベースに項目が無いので、代わりにその火山自身の
  **VEI 分布・最大 VEI・VEI 4 以上の回数・1500 年以降の噴火間隔**を示す。
  正本は [`docs/VOLCANO-INTELLIGENCE.md`](docs/VOLCANO-INTELLIGENCE.md)。

### 3.3 分析・ツール

- **統計 (Stats)・情報 (Information) ダッシュボード・コミュニティ・Companies** タブ。
- **企業アトラス**——企業を選ぶと、その企業のプロフィール（正式名・本社・代表者・設立・従業員・
  売上／営業利益／純利益／総資産・上場市場と識別子・主要製品・親会社と子会社・進出国）と、
  **世界の実在拠点**（本社・事業所・工場・研究開発・物流）が地図に出る。数値は必ず**通貨と年度**を、
  施設は必ず**出典と位置精度**を持つ。⚠ **公表されていない値は埋めない**——空欄はデータの状態であって
  不備ではない。正本 [`docs/COMPANIES.md`](docs/COMPANIES.md)。
  ⚠ **企業ロゴは、閲覧時に外へ取りに行くものではない。** Wikidata の公開ロゴ（Wikimedia Commons）を
  **ビルド時に解決して同梱**してあり、それが無い企業は**頭文字のマーク**になる——ロゴが出ない行は
  不備ではなく、「公開されたロゴが登録されていない」というデータの状態である
  （`docs/COMPANIES.md` §4.3）。
- **多国比較 `IntMapStatsCompare`** — 最大10か国 × 28指標、**棒グラフ／時系列／表**の3モード、
  出典切替（世界銀行 ⇄ IMF WEO）、CSV エクスポート。
- **測る系** — 距離・面積・半径・自由描画・3-D 体積・標高断面・見通し線 (viewshed)・
  到達圏 (isochrone)・海路ルート。
  ⚠ **地点についての答えを出すツールは、地点を勝手に決めない。** 一覧から開いたときは指の下に座標が
  無いので、**答えそのものが1つの座標の関数であるもの**（見通し線・到達圏・星空・**日照時間と影**）は
  地図をタップさせ、取り消せば何も開かない。訊くのは**このアプリが元から使っている赤いトースト**
  **ひとつだけ**——同じ文を #R196 の共有バーにも同時に出していたので、1回の操作で同じ一文が
  2つの別々の見た目で画面に並んでいた。バーは黙って構える（`announce:false`）。専用の UI は作らない。
  ⚠ 十字カーソル・パネルの透過・Esc・取り消しは**どちらの経路でも同じ**——消えたのは2つ目の写しだけ。
  日照パネルのように**自分の文章欄を持っている**ものは、そこに1回だけ書く。
  **地点が無くても見せるものがあるもの**（地形編集・水流、ドローン飛行計画）は訊かずに開き、
  自分のパネルの中で地点を名乗り、選び直せる。地図の右クリックから開いた場合は、そこが地点である。
  ⚠ **地点が無いあいだ、数字も影も出さない。** 日照パネルは地図中心を観測地点として計算していたが、
  それは誰も選んでいない場所についての答えである。いまは「地点を選んでください」だけを出す。
  ⚠⚠ **そして毎回訊く。** 前のツールで選んだ地点も、落としたピンも、次のツールの答えではない
  ——一度選ぶとその後のツールが全部その座標で黙って開いていたので、**一覧の行は必ず地図をタップ
  させる**（記憶した地点もピンの代用も、この経路には残っていない）。地図の右クリック・Atlas・
  各パネルの ◎「地点を変える…」は今までどおり、**名指しされた**座標でそのまま開く。
  **到達圏**は Valhalla／OpenStreetMap の**実道路網**に沿った時間到達域（車・徒歩・自転車、
  1〜120分・最大4本の等時線）で、**距離の円ではない**。**公共交通**も同じパネルの4つ目の交通手段で、
  そのときだけ OSM 鉄道網の Dijkstra（`IntMapTransitReach`）が答える——鉄道モデルは持ち時間を1つしか
  解かないので、選んだ時間のうち**最大値**を使い、そう画面に書く。1点をクリック（🎯 パネル）でも
  Atlas（`isochrone`）でも同じ実装を呼ぶ。算出できなかったとき・地図に描けなかったときは
  **成功とは報告しない**（ルータ側の失敗と地図側の失敗を区別して言う）。
- **範囲人口** — 面積測定・半径円・自由描画のパネルと Atlas `population` から、**WorldPop 100m グリッド**の
  実グリッド集計で「囲んだ範囲の人口」を出す。**面積によらず常にタイル分割**するので、進捗バーの割合は
  常に実測値（`done/cells.length`）であり、アプリの他の進捗バーと同じ確定バーである。
- **物理シミュレーション** — 地震波（`js/seismic.js`）、津波（`js/tsunami.js`。**地震シミュレーターから
  渡す**もので、単独のボタンは無い）、地形編集と水の流れ（`js/terrain-water.js`）、
  **放射性物質の拡散**（放出量・核種・放出時間・追跡時間を読み手が決めて実行する。実風の場での
  ラグランジュ拡散）、日照と地形の影、**電波・通信圏／見通し線**（1つのパネルの2つの解析——幾何だけの
  可視領域と、k=4/3・周波数・送信出力から到達距離を決める受信域）。
  手法の説明は設定 ▸ 科学とロジック。
- **ドローン航法** — Measure ▸ Drone。DEM を経路に沿ってサンプリングして総飛行距離・立体経路長・
  予想飛行時間・地上高・最大必要高度・推定バッテリー消費と、**飛行条件を満たさない地点とその理由**を返す。
  機体条件はすべて編集可。高度は AGL/AMSL をウェイポイントごとに保持して両方表示する。
- **経路 (Directions)** — **Layers ▸ Tools ▸ 経路**から開く。車／公共交通／徒歩／自転車
  （OSRM・Valhalla・Transitous / MOTIS）。地点は**候補を見せて選ばせる**（勝手に1件に確定しない）。
  経由地の追加・並べ替え・旅程ごとの逆転、代替経路のカード（地図の線と相互に選択）、
  車線案内付きの手順、公共交通のリアルタイムと遅延、回避条件と通過禁止範囲、GPX / GeoJSON 出力、
  共有リンク。分析（標高・国境通過・沿道の天候と災害・到着時刻・経路差・過去の路線網）は別タブ。
  **パネルを閉じると地図の経路も消える**（地点は残るので開き直せば同じ旅程がそのまま出る。
  パネルを開いたまま地図だけ消すのが「経路を消去」）。候補を選ぶと**そのカードが開いて**中に手順が
  出る——**候補が1本のときも、Atlas の返信の中でも同じ形**（surface で見た目が変わらない）。
  地点欄は**地名で検索できる**（内蔵の地名辞書がネットワーク無しでも即座に候補を出し、
  そこへ Open-Meteo と Nominatim の答えが併合される。CJK は1文字から引ける）。
  **「経路を検索」ボタン**があり、押せないときは**なぜ押せないか**が同じパネルに1行で出る。

- **案内 (Navigation)** — 引いた経路の上を**実際に連れて行く**。現在地を追い、経路に合わせ、
  曲がる場所を出し、残り時間・残り距離・到着時刻を毎秒更新し、必要なら音声で言い、
  **道を外れたら自動で引き直し**、経由地と目的地への到着を検出する。
  カメラは進行方向を上にして現在地を画面の下寄りに置き（手で動かすと追従は一時解除、
  ボタン1つで戻る）、案内中は経路パネルではなく**専用の画面**になる。
  Atlas からも同じことができる——「このルートで案内開始」「あと何分？」「次の曲がり角は？」
  「全体表示」「音声を止めて」。**Atlas は独自の状態を持たず、画面と同じ store を読む。**
  ⚠ **正直さ**: 交通情報を持つ provider が使えないとき、所要時間は「交通状況未反映」と明示する。
  レーン案内は provider が車線を返したときだけ出る（推定しない）。
  ⚠ **位置は経路を要求するときしか端末を出ない**——追従も進捗も到着判定も手元で計算する。
  デスクトップではパネルを**掴んで動かせ・端でも角でも大きさを変えられ・ヘッダだけに畳める**、
  位置と大きさと畳んだ状態は覚える。**地図の線を押すとパネルが開いてその候補が選ばれる**
  （カメラは動かない）。時刻は**その地点の現地時刻**（設定でタイムゾーンを指定していればそちらが優先）。
  どの地点欄からも **◎ で現在地**を入れられる（許可を求めるのは押した瞬間だけ）。
  携帯では3段のボトムシート。
  ⚠ **ライブ交通を持つプロバイダーは1つも無い**ので、道路の所要時間は常に「標準所要時間・
  リアルタイム交通量は未反映」と書く。Atlas の自然言語も同じ状態を読み書きする。
- **Playground (beta)** — Layers ▸ Tools から起動。World Explorer（衛星版 GeoGuessr）／
  Pandemic Simulator（国別 SEIR メタ個体群）／Statecraft（1900–2026 の国家運営）／World Sandbox／Quiz。
- **フライトシミュレーター**、**宇宙を探索**（`js/space.js`）、**夜空**（`js/night-sky.js`）。

### 3.4 Atlas（自然言語コンソール）

- **Atlas は1手ごとに選ぶ。** 自然言語を受けて、その場で**最終回答を書くか、IntMap の
  道具を呼ぶか**を選び、呼んだときは**機械的な実行結果**を受け取って次を選ぶ。
  **道具を1つも呼ばないのも正式な終わり方**で、普通の質問は普通に答えて終わる
  ——地図は1ミリも動かない。**最後の1文は、何が起きたかを読んだあとに書かれる。**
- **使える道具は、そのターンに必要な分だけ渡される。** 中核の数ツールと、「これができるか」を
  **検索する道具**。130 の能力すべてに届くが、毎回全部を見せるのではない。
  ⚠ **渡す量は減らすが、届く範囲は削らない。** 検索は得点したものを**全部**返す——以前は上位 8 件で
  切っており、同点は ID のアルファベット順だったので「現在地から大阪駅までの経路」で
  **経路そのものが 9 位になって落ちていた**。
- **地図は道具であるだけでなく、回答の形式でもある。** Atlas は返答ごとに、その回答が**文だけ**か、**地図が回答**か、
  **両方が分担する**かを自分で宣言する。地図が回答だと宣言したのに何も描いていない返答は、読者に届く前に
  Atlas 自身へ差し戻される——「地名が出たら地図化」という古い義務は無く、**自分で決めたことを守らせるだけ**。
  そして「地図で説明する」は 1 回の操作である: 地点を番号順に置き、それぞれが答えの中で何であるかを添え、
  地点間の流れを弧で結び、地域を塗り、全体が収まるようにカメラを合わせ、同じ番号の凡例を出す。
  回答文の中の地名には同じ番号が付き、文をなぞれば地図の印が光り、印をなぞれば文が光る。
  解決できなかった地名は**名前で**「配置できなかった」と読者に告げる——座標を発明しない。
- **最初に出る4つの例文は、いま見ている地域について「実際に成り立つ事実」から選ばれる**。
  固定文に国名を差し込むのではなく、候補群の中から**その国について真であるものだけ**が候補になる——
  人口密度・GDP・面積・国防費比・HDI・寿命・ネット普及などの**世界順位**、
  **利用者が今オンにしているレイヤー**（気象警報・地震・風・火山・海底ケーブル…）、
  **Chronos の位置**。モンゴルとバングラデシュはチップが1つも重ならない。
  海の上や、1つの国を主題と呼べないほど引いた表示（z<2.5）では**世界規模の候補群**に戻る。
  カメラが落ち着いてから 600 ms 後、レイヤーを切り替えたとき、Chronos を動かしたときに、
  **出る4つが実際に変わるときだけ**引き直す。
- **条件を複数まとめた問いに、行で答える。** 「人口100万人以上・年降水量500mm未満・海から200km以上・
  過去30日にM5以上の地震があった都市」のような問いは、IntMap が**自分のデータを絞り込んで交差**させ、
  **表**と**地図上のピン**で返す。表の下には**どの列がどのデータセットから来たか**が必ず出る。
  使える表は 都市（GeoNames 147,924 件）／国（Countries の統計）／地震（USGS）／火山（Smithsonian）／
  施設（OpenStreetMap＋Wikidata）。列は 人口・年降水量・**海からの距離**・標高・現在の気温／風速／湿度・
  国の統計（1人当たりGDP・HDI・出生率…）・**任意の World Bank 指標**。
  ⚠ **これは「調べて文章を書く」ではない。** 条件はコードが実際に評価する。**評価できなかった条件が
  あれば表の上に出る**し、打ち切った件数も隠さない。該当 0 件は「該当 0 件」と答える。
  ⚠ **「海」の定義は 2 通り出す**——外洋までの距離と、カスピ海のような内海を海に数えた距離。
  テヘランは前者で 611 km・後者で 109 km なので、この 1 語で答えが変わる。
- **選択肢を提示した場合、回答した瞬間に選択 UI は消える**（質問文と選んだ答えは残る）。
- **返答の中の数・時刻・略語は、そのまま引ける。** `120 miles` に触れれば `≈ 193 km`、
  `14:30 UTC` なら読者の時間帯での時刻、`EEZ` なら正式名称と一文の意味（9言語・34語）。
  ⚠ **単位の綴りをロケールの約束どおりに読めないときは、推測せず注釈を出さない。**
  丸めた値には `≈` が付き、ちょうどの値には付かない。
- **進行中の表示は、ラベル（Thinking / Searching / Analyzing / Mapping / …）自身を採くシマー**である。

- **UI のボタンと Atlas の自然文は、同じ実行カーネルを通る。** 「IntMap が何をできるか」の一覧は
  **1つだけ**あり（能力レジストリ・130 能力・440 の綴り（ID＋別名、重複を除いた実測））、ボタンもテストも監査も同じ能力 ID を
  名指す。**能力は、そのモジュールが読み込まれる前から発見できる。**
- **「完了」は観測した事実であって、関数が返ったことではない。** すべての操作は
  実行の前後でアプリを観測し、宣言した事後条件を満たしたときだけ完了と名乗る。
  満たさなければ `partial`（一部だけ）・`running`（計算中）・`needs_input`（入力待ち）・
  `failed` のいずれかを、その理由とともに返す。
  ⚠ **地図を求められた依頼は、地図に何も出ていないうちは完了しない。**
- **必要な地点を勝手に決めない。** 地点・範囲・線が要るツールに対象が渡されなかったときは、
  **地図の中心で代用せず**、その場で訊いて、答えが来たら**同じ操作を再開する**。
- ⚠ **ただし「訊く」の前に「取りに行く」がある。** 利用者の**現在地**は Atlas が自分で取得する
  （`my_location`。ブラウザの許可ダイアログは端末が出す）。「現在地から大阪駅まで」は、
  利用者に住所を打たせる依頼ではない。**現在地は 9 言語すべてで言える**——
  「現在地」「my location」「ma position」「내 위치」「我的位置」「当前位置」ほか。
  取得できなかったときは**地図の中心で代用せず**、取得できなかったと言う。
- ⚠ **Atlas は地図を「見る」ことができる。** 状態（中心・zoom・ON のレイヤー・選択・時刻）は
  以前から読めていたが、**画素は見えていなかった**。`look_at_map` は読者がいま見ている画面を撮り、
  次の一手でそれを**画像として読む**——「この黄色い帯は何？」「この境界線おかしくない？」
  「このラベル何て書いてある？」「点が密集してるのはどこ？」「レイヤーは本当に描画された？」に
  答えられるのはこれがあるときだけ。**凡例・スケール・マーカー・ニュース帯まで含む画面**と、
  **地図のフレームだけ**の 2 通りがあり、どちらを撮るかは Atlas が決める。カメラを動かしたあとに
  もう一度見て、自分の操作の結果を確かめることもできる。⚠ **数値は画像から読まない**——座標・zoom・
  レイヤー名・日付は機械値として同時に渡されるので、画像は「どう見えるか」にだけ使う。
  ⚠ **撮った絵は読者にも見せる**（回答の中に縮小版・タップで拡大）。Atlas が何を見て答えたのかが
  読者に分かる状態でだけ、この機能は成立する。
- **1つの依頼は AI 利用枠を1回しか消費しない。** 計画・修復・再読み取りは1つの依頼の内部工程で、
  以前はそれぞれが1回ずつ消費していた。決定論的に終わる依頼は枠を一切使わない。
- **Atlas が IntMap を使う。逆ではない。** 依拠するものの順位は、①利用者の目的 ②正確性・最新性・
  有用性 ③Atlas 自身が選んだ情報源や方法 ④IntMap 内部のデータ・プロンプト・カタログ——**内部データは
  最後**である。薄い・古い・偏っている・訊かれたことと無関係・より良いものと矛盾する内部データは
  **推論から外してよい**し、IntMap のデータを一切使わずに答えてもよい。内部と外部が食い違ったときは
  内部を守らず、日時と信頼性を比べて妥当なほうを採り、**食い違ったことと採った理由を述べる**。
- **地図製品であることは、地図を触る理由にならない。** 依頼が操作を求めているとき、または地図が答えの
  一部を担うとき（どこにあるか・どれくらい離れているか・どう分布しているか）に地図を使う。説明・定義・
  計算・歴史・コード・翻訳のように**答えが文章であるものには、画面操作を一切足さない**。
  ⚠ これは Atlas 自身の判断である——以前はコード側の門が「地図の動詞が無ければ視点操作を落とす」と
  決めており、その門は「台湾へ旅行したい」のような依頼を落としていた。⚠ ただし**旅行・旅程は地図の意図**
  として扱う（「台湾へ旅行したい」には地図の動詞が1つも無い）。
- **報告書は、頼まれたときだけ出す。** 「ブリーフ」「レポート」「報告」等の明示要求が無いのに planner が
  `research.brief` を選んだ場合は、**validator が `analyze` に差し替える**（消さずに差し替える——
  質問には答えが返らなければならない）。
- **一度得た座標を捨てない。** 地点は ID・名前・緯度経度・種別・日時・出典・確度と **provenance** を
  持つ1つのオブジェクトで、回答・出典カード・地図のマーカー・選択状態が**同じもの**を指す。
  ⚠ **国や地域の代表点は「地点」ではない**——利用者が指した点として扱わず、そこに何があるかを語らない。
- **IntMap の全機能に到達できることが恒久ルール**。到達経路は3つ：
  **DOM 操作**＝`control` ＋ `controlCatalog`、**レイヤー**＝`layer` ＋ `layerCatalogText`、
  **モジュール**＝`module` ＋ `moduleCatalog`（`window.IntMap*` を**自動発見**し、
  許可リストで制限したメソッドを名前で呼ぶ）。**新しく足したモジュールは配線なしで到達可能**になる。
- **カタログに書かれていない操作は、プランナーにとって存在しない。** `SYS()` が渡す操作カタログが
  モデルの知る全世界なので、ディスパッチャに `case` があってもカタログに項目が無ければモデルは
  それを出力できず、**似ている別の操作で代用するか「その機能はありません」と答える**。
  この規則自体は長く明文化されていたが検査が無く、実際に6つの機能（到達圏・複数地点最短巡回・
  オブジェクト一覧・傾斜／斜面方位・電波到達域・世界の巻き戻し）が実装済みのまま不可視だった
  （うち「世界の巻き戻し」と「傾斜／斜面方位」はその後、機能ごと撤去した）。
  **`npm run check:catalog`（`scripts/atlas-catalog.mjs`・`npm test` に内包）が、
  ディスパッチャの `case` 群とカタログ本文を毎回突き合わせて落とす。**
  意図的に撤去した機能だけが例外で、理由つきで `WITHDRAWN` に明記する（現在は `monitor` のみ）。
- **実行結果は検証して正直に報告する。** 各アクションは構造化された `{ok,html}` を返し、
  効果（チェック状態・テーマ値・ハイライトの描画結果）を検証する。失敗は注意色で明示し、
  `run()` が実結果を集計してモデルの楽観的な `say` を上書きする。
- **返答は利用者のメッセージの言語**（UI 言語ではない）。判定できない言語は UI 言語へ落ちる。
- **画像入力（ビジョン）** — 入力欄に画像をペースト／ドラッグ／添付できる（最大4枚）。画像は
  地図志向のプランナーを経由せず `_atlVisionTurn` が **分類（`contentClass`）→ 転記 → 求解 →
  決定論的検算 → 統一レンダリング → 地理のときだけ地図化** を行う。**非地理クラスでは地点抽出を
  一切呼ばない**ので、数式や文書の語が地名に化けることが構造的に起きない。
- **返答の描画** — `js/atlas-reply.js` が安全な markdown（`IntMapSafe` でエスケープしてから整形）・
  KaTeX の数式・コードブロック・GFM 表・出典リンクカードを描く。
- **主なアクション** — 移動／ズーム／投影／3D／ベース地図／方位、レイヤー切替と不透明度、
  `highlight`（国・行政区分・通称地域の実境界）、`value`、`clear`、`locate`、`poi`（Overpass ＋ Wikidata）、
  `analyze`（実データを集めた統合分析）、`brief`、`answer`、`mapReport`（ライブニュース事件の地図）、
  `researchMap`（歴史・現在・混合のリサーチ）、`compareStats`、`mapMetric`、`scoreMap`、`explore`、
  `impact`、`population`、`drone`、`engine`、`module`、`control`、
  `isochrone`（**到達圏**——実道路網に沿った時間到達域。`radius`＝直線距離の円とは別物で、
  「徒歩1時間で行ける範囲」のような**所要時間**の質問には `radius` で代用しない）、
  `optimizeRoute`（複数地点の最短巡回）、`objects`（オブジェクト一覧）、
  `rfCoverage`（電波到達域——`los` と同じ可視領域モデルに周波数と送信出力を与えたもの）。
  ⚠ **`earthReplay`（世界の巻き戻し）は撤去した。** 1つの時計で地球全体を過去へ動かすのは Chronos の
  仕事で、地図の年を動かすのは `timeTravel`。同じ状態への2つ目の扉だった。
  ⚠ **`slope`（傾斜・斜面方位）も撤去した。** レイヤー本体（`⛰ 傾斜・斜面方向`）を消したので、
  ディスパッチ・能力表・スキーマ・カタログ本文のどれにも残していない。
- **「所要時間」と「距離」は別のアクション。** `radius` は直線距離の円、`isochrone` は
  Valhalla／OpenStreetMap の**実道路網**を辿った到達域（`auto` / `pedestrian` / `bicycle`、
  `transit` は OSM 鉄道網で到達可能な駅）。起点は地名・`現在地`（端末のGPS）・クリック地点・
  `lng`/`lat` のいずれでもよい。**カタログの `radius` の項目自身が、所要時間の質問を
  `isochrone` へ回すよう書かれている。**
- 到達圏の依頼は Atlas が読む——漢数字・全角数字も、「〜で行ける範囲／エリア／到達圏」も
  「〜分以内」も、言い方の一覧をコードが持つのではなく Atlas が意味で取る。
  ⚠ **以前は `localPlan` という決定論パス（154 の正規表現）が AI の前でこれを判定していた。**
  手段の無い「30分の範囲」が黙って落ちるといった穴は、その表の穴であって依頼の問題ではなかった。
  引き換えに、ログイン前・枠切れ時の即実行は無くなった（日次枠の案内を出す）。
- **リサーチ地図の二系統** — `mapReport` は**現在・直近のライブニュース事件のみ**、
  `researchMap` は歴史・現在・混合のリサーチで文章と地図を独立に生成する。
  **Request Profile**（時間軸・地理・要求出力を機械判定）と能力レジストリによる実行前プラン検証で、
  歴史質問がライブ専用の `mapReport` へ流れるのを実行前に防ぐ。
- **座標・URL・出典をモデルに生成させない**。`mapReport` は IntMap が集めた証拠に ID を付け、
  モデルは `{name,locationName,country,summary,date,evidenceIds}` だけを返す。クライアントが証拠 ID の
  実在を検証し、地名を geocode して位置を確定し、URL・出典・日付は**引用された証拠から**充填する。
  確認できない位置はピンにせず一覧だけにする。
- **回答文の語句をその場で引ける** — 回答の中の語句を選んで**右クリック**（タッチは長押し →
  「解説」）すると、小さな辞書カードがその場に開く：**意味**・**この文での意味**・**背景**・**関連語**。
  価値の中心は 2 番目で、**その段落を持っている側にしか出せない**——同じ `Georgia` が国か州かは
  語ではなく文脈が決める。⚠ **質問の回数は減らない**（専用枠。free 60 回/日）。
  同じ語×同じ回答はキャッシュするので訊き直しは無料。Atlas に「この言葉の意味は」と言っても
  同じカードが開く（`Architecture.md` §2.1b）。
- Atlas は通常モードでは **サイドバーの本物のタブ**（`#atlas-feed`）で、ワークスペースモードでは
  専用ウィンドウで開く。カーネル `js/atlas-console.js` は**押されてから取りに行く**（`Architecture.md` §9.3）。

### 3.5 アカウント・その他

- **アカウント／AI機能** — ログインで使えるアカウント制AI（1日上限つき。`Architecture.md` §5）。
- **テーマ** — System / Light / Dark ＋ **アクセントカラー**（アカウント同期）。
- **設定ページ** — 通常／モバイル／ワークスペース共通のモーダルを **8セクションの iOS 風グループ**に
  分けたもの（外観／レイアウトとパネル／地図の動作／単位と時刻／ニュースとティッカー／AI／連携・キー／
  情報とサポート）。
- **ウィンドウ・ワークスペース**（デスクトップ・既定オフ） — 地図・Information・News・Countries・
  Layers・Atlas が自由配置・全辺リサイズできる本物のウィンドウになる。レイアウトは localStorage に永続化。
- **下部ティッカー**（既定オフ・デスクトップのみ） — FX・株価指数・金銀・暗号資産・読み込み済みニュース
  見出し。表示銘柄は設定で選べる。`body.ticker-on` を flex 列にしているので地図に重ならない。
- **天気ポップアップ**（右クリック → 「ここの天気」） — Open-Meteo の現況＋5日予報。
- **Bug Report** — 診断情報を自動添付して Supabase `bug_reports` に送信（オフライン時はローカル＋
  クリップボード）。**フィードバック**、**寄付 (Stripe)**、**管理コンソール (`admin.html`)**。
- **多言語ニュース翻訳**（任意）。

### 3.6 ウィジェット板（サイドバー）

タブを開いていないときサイドバーに出る、**48種類のカードから組む自分用の盤面**。
仕組みは `Architecture.md` §7.5、ファイルの分担は [`docs/FILES.md`](docs/FILES.md) §3。

- **盤はスクロールするし、カードは自動で敷き詰まる。** 幅の狭いカードの隣に空きができるときは、
  あとのカードのうち入るものを前へ引いて埋める（**後ろへ押し戻すことはしない**）。
  並びは見た目だけでなく DOM も同じ順なので、キーボード操作と読み上げの順序が画面と一致する。
- **消したカードは戻ってこない。** アカウント同期は盤を**モジュールから読む**ので（保存キーを
  直接読まない）、空の盤も空のまま往復する。
- **サイズは S / M / L で、情報そのものが変わる。** 大きくしても同じ内容が拡大するのではない
  ——時計の L は都市の一覧と昼夜、進捗の L は日・週・年を1本の軸に、天気の L は7日の実データの推移、
  地震の L は一覧と位置図。**そのサイズで意味のあるものだけ**を出す。
- **追加はギャラリーから。** 検索・9カテゴリ・**実際のカードと同じレンダラで描くプレビュー**・
  サイズ切替・追加前の設定。プレビューは通信も位置情報の要求もしない。
- **カードの中で操作が完結する。** レイヤーの ON/OFF、地図へ移動、地点の変更、座標のコピー、
  国のウォッチ追加、Chronos を現在へ戻す、記事を開く——見て終わりにしない。
- **状態が言葉で出る。** 読み込み中は完成形と同じ寸法のスケルトン、失敗は理由と再試行、
  上限は「上限に達した」、権限は理由と許可ボタン、静かな日は「該当なし」。
  ⚠ **取得に失敗しても前回の値は消えない**（何分前の値かを添えて出す）。
- **同じ問いは1回しか聞かない。** 同じ地点の天気カードを何枚置いても要求は1つ。
- **スタック**——複数のカードを1枠に重ねてスワイプで切り替える。**Smart Stack** は文脈
  （重大警報・実行中の経路・選択中の国・現在地・地図の範囲・Chronos・時間帯・直近使用）で前面を選び、
  **なぜ表示されたかを答える**。乱数ではない。
- **IntMap にしか作れないカード** — 保存地点の警報／表示範囲の状況／地図上のニュース／国のウォッチ／
  有効レイヤー／地域監視／経路の状況／Atlas ブリーフィング／Chronos。
  いずれも**アプリが既に持っている正規化済みのデータを読む**（第2の実装を作らない）。
- ⚠ **Atlas ブリーフィングのカードは AI を自動で消費しない。** 読み手が Atlas に頼んだ結果を
  表示するだけで、カード自身が問い合わせる経路は存在しない。
- ⚠ **これは Web ページの中のカードであり、iOS のホーム画面／ロック画面ウィジェットではない。**
  ネイティブのウィジェットは今回作っていない。

---

## 4. Atlas — Central OS Vision（恒久指針）

> ユーザー指示（2026-07-11）の全文骨子。**到達点であって現状ではない。** 現在どこまで実装されて
> いるかは、[`Architecture.md`](Architecture.md) §2 と `node scripts/atlas-catalog.mjs`（全アクションの一覧）が
> 実体から答える——ここに実装対応表を置くと、その表だけが古くなる。

### 4.1 20項目の要求（要約）

1. **IntMap全体の自然言語操作** — 全操作を自然言語から。複数指示の順次処理。「実行しました」ではなく実際の状態変化を確認し、失敗は失敗として理由と代替を示す。
2. **現在のIntMapの状態理解** — 地図中心/範囲/ズーム/投影/ベース/有効レイヤー/透明度/選択国/表示記事/比較対象/時間範囲/ピン/アウトライン/ハイライト/半径/描画/計測/開いている画面。UI直接操作もAtlasが認識（状態は共有、別系統にしない）。
3. **会話の文脈理解** — 対象地域・比較対象・注目問題・期間・直前データ・除外条件を維持。ただし現在の指示と地図状態を優先し、結果が大きく変わる曖昧さのみ確認。
4. **地図・ニュース・統計の統合** — 問いの理解に必要な情報だけを選択し、地図表示と文章説明を連動。
5. **世界を共通の対象として理解** — 「台湾」「一つの地震」「一つの記事」を、位置・境界・統計・ニュース・施設・関連イベントが結び付いた同一実世界オブジェクトとして扱う。
6. **記事ではなく出来事を扱うニュース**（**到達**） — 同一出来事の複数報道を統合し、続報・訂正・情報源間の相違を更新し続ける。News が既定で出来事単位になり、Atlas は `research.events` と `news.category` で出来事を主語に扱う（`docs/NEWS-EVENTS.md`）。
7. **目的から調査を組み立てる** — 質問を作業に分解し、進捗・欠損を把握し、情報源が死んでいれば代替を試し、それでも欠ければ明示して可能な範囲で分析。
8. **現在の世界の統合分析** — ニュース検索だけで答えない。最重要の変化を最初に。見つからなくても即「何もない」とせず、範囲・期間・言語・語を変えて確認。
9. **時間の統合** — 二時点比較、期間内の重要変化、変化直前の出来事。「以前と比べて何が変わったか」が基本の問い。
10. **地理的な関係の発見** — 指標間の関係探索。相関・順位相関・時系列前後・地域傾向・交絡・外れ値・欠損を考慮し、断定せず「確認できた関係/考えられる説明/反証/限界」を分けて示す。
11. **地理的影響の分析** — 出来事の影響が「どこへ広がるか」をIntMap上で示す（地震→人口・原発・港湾、海峡封鎖→航路・輸入国・代替経路…）。
12. **シナリオ分析** — 条件付き影響分析（前提・データ・考慮外・不確実性を明示。予言としない）。
13. **独自レイヤーの生成** — 複数指標から新しい評価レイヤーを構成し、会話で条件変更。
14. **動的な画面構成** — 目的に応じた情報配置（既存タブは構成要素化）。※当面の最優先ではない。
15. **情報の信頼性管理** — 出典・取得日時・対象期間、事実/推論/推計の区別、情報源間の相違の明示。
16. **Atlasの自己確認** — 操作後に出力を検証（レイヤーは本当に表示されたか、同名地の取り違えはないか、単位・期間・国コードの整合）。実行後に誤りを発見して修正できることが知能。
17. **IntMap自体の診断** — データ更新停止・API障害・異常値・操作と表示の不一致の検出。新機能はUIとAtlas両方から到達可能かを確認。
18. **Atlas中心のIntMap** — 問い→対象/時間/データ判断→地図構成→収集→分析→地図と文章の連動→会話での条件変更。
19. **実現段階** — 第1段: 全既存操作の確実な実行（発言と状態の一致、同名地・非同期・レイヤー残留・競合の解消）／第2段: 地図状態・選択対象・会話文脈の完全理解／第3段: ニュース・統計・地理・リアルタイムの統合（操作層→調査層）／第4段: 出来事単位のニュース・時間変化・影響範囲・情報源相違／第5段: 関係探索・条件付き影響分析・独自レイヤー生成／第6段: 新機能の自動認識（Central OS構造の完成）。
20. **当面の優先事項** — 保存/外部出力/PDF/プレゼン/高度監視は後回し。最優先5つ:
    1. 全操作の確実な実行
    2. 地図状態と会話文脈の完全理解
    3. ニュース・統計・レイヤー・リアルタイムの横断結合
    4. 操作結果と分析根拠の自己確認
    5. 新機能が自然にAtlasへ統合される構造

### 4.2 最終定義

Atlasは、世界について質問すると答えるAIではない。ユーザーの目的を理解し、IntMapの全状態を認識し、
必要な機能とデータを選び、複数の操作を実行し、場所と時間を軸に情報を統合し、地図上で世界を理解可能な形へ
構成する **IntMapのCentral OS** である。IntMapはAtlasが世界を観測し、分析し、操作するための身体になる。

> **構造化の起点（R82）**: この「Atlasの上でIntMapが動く」を*構造*として実装開始 — `window.IntMapOS` カーネルを新設し、
> GUIとNLチャットを**同一カーネルへインテントを投げる薄い2つのシェル**に。地図ビュー+タブは真にインバート済み
> （UI→カーネル→エンジン、Atlasのclickid模倣を排除）。全操作面はcatalogに登録され、以降のパスで残りコントロールを
> 順次コマンド化していく（第六段階の完成へ）。詳細は DEV-NOTES R82。

**標準ルール（既存のCONSTITUTION/メモリと同じ効力）**: 新機能を追加する変更は、同じ変更の中で
dispatch/カタログへ配線し、状態を stateContext に載せ、失敗を正直に報告し、可能なら実行後検証を持つこと。
