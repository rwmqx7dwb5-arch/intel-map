# IntMap — 決定 (Decisions)

> **今も有効な技術判断と、その理由だけ。** 「どう実装されているか」は
> [`Architecture.md`](Architecture.md)、「いつ・なぜそう変えたか」は `DEV-NOTES.md`。
> ここにあるのは、**次に同じ問いが来たときに再検討しなくてよいもの**である。
>
> - **正本**: このファイル。判断が覆ったら**行ごと差し替える**（覆った記録は `DEV-NOTES.md` が持つ）。
> - **読む人**: 「なぜこうなっているのか」を知らずに変えようとしている人。
> - **更新条件**: 新しい判断をしたとき、既存の判断を覆したとき。**追記して両方残さない。**
> - ⚠ **理由の無い行は書かない。** 理由を書けない判断は、まだ判断ではない。

---

## 配信とビルド

| 決定 | 理由 |
|---|---|
| **公開は GitHub Pages、公開物は `dist/`** | 静的サイトで足りる（サーバー側の処理は Edge Functions 側にある）。ソースツリーをそのまま配ると、ビルド後にしか存在しないものが本番で欠ける。 |
| **公開は CI ゲート付きの Actions（`deploy.yml`）を通す** | 「ビルド → 静的検査 → 公開 → 実 URL へのスモーク」を1本にしないと、壊れた版が本番に着地したことに**着地してから**気づく。 |
| **ビルドは Vite**、`src/main.js` が `js/*.js` を index.html と同じ順で import | ハッシュ付き・最小化・チャンク分割を素の HTML では得られない。順序を保てるのは、`js/` の全ファイルに**トップレベル宣言が1つも無い**ことを AST で毎回確かめているから。 |
| **配信入れ替え直後の「古い文書」からの復帰は `index.html` のインライン script で行う（Service Worker に navigation を持たせない）** | 直せる場所が3つあり、2つは使えない。①**ヘッダ**——GitHub Pages は `max-age=600` を**全応答に一律**で返し（名前にハッシュを持つ不変資産も同じ値）、ファイル単位の指定手段が無い。②**Service Worker**——`sw.js` を登録するのは `js/tile-warm.js`＝**バンドルの中**なので、この失敗に遭う読者が worker を持っているとは限らず、**守れる範囲はインラインの復帰より狭い**。そのうえ navigation を fetch ハンドラに通せば**温まった起動すべてに往復が1回増え**（`Architecture.md` §1.1 の起動予算）、そのハンドラの不具合は**戻ってくる読者全員を締め出す**——404 が起きた後にしか動かないリスナーとは影響範囲が桁違い。③**インライン**なら欠けている `assets/` に依存せず、健全な起動には1バイトも足さない。 |
| **本番にソースマップを出さない** | 出すと全ソースの完全な写しを配ることになる（実測 8.8 MB あった）。 |
| **実行時依存は npm から取り、`src/vendor.js` が同じグローバル名で再公開する** | CDN の浮動タグ（`@2` のような可動タグ）は「次に何が公開されても実行する」という約束になる。⚠ `index.html` の CSP に残る CDN ホストは計測・地図系の**既存**のタグで、これは残存リスクとして追跡している（`docs/SECURITY-ARCHITECTURE.md` §8）。 |

## 地図

| 決定 | 理由 |
|---|---|
| **既定のレンダラは MapLibre GL、Cesium は設定で選べる第2エンジン** | 既定セッションに Cesium のコストを1バイトも払わせない（動的 import・参照0）。それでも「本物の球」が要る用途があるので、消さずに選択肢にしてある。 |
| **レンダラの名を出してよいのは `js/geo-engine.js` だけ** | 2つ目のエンジンを足せたのはこの制約のおかげ。契約（`layers`/`camera`/`coords`/…）以外を触ると、片方のエンジンでだけ動くコードが静かに増える。 |
| **レイヤーのトグルでカメラを動かすのは、データが1つの地域にしか無いレイヤーだけ** | #R21 が入れ、#R30 が「1px たりとも動かさない」のために削除してトーストに置き換えたものを、利用者が**範囲を限って戻した**。他の場所でオンにしても空の地図しか出ないレイヤーでは、トーストは**地図の仕事を言葉で代行していた**。対象は `js/layer-home.js` の表 1 か所に限定し、**セッション中1回・利用者が操作したときだけ**。各レイヤーのファイルに `fitBounds` を散らすと、「1回だけ」の定義がファイルの数だけ増える。 |
| **レイヤー内の表現を個別に止められるようにする（風の粒子）** | 1 つの場から**色ラスタと粒子の2つ**を描くレイヤーで、アニメーションを止める唯一の手段が「レイヤーごと消す」だと、止めたいもののために見たいものを失う。切り替えは**凡例の中**（#R16）で、**rAF ループごと止める**——見えないだけで計算が回り続けるのは「オフ」ではない。 |
| **基図の帰属表示は地図カラムの最下「行」にする（`#map-credit`・オーバーレイにしない）** | CARTO の Basemap Terms が無料枠の対価として "prominent and conspicuous to Persons viewing each CARTO basemap" を求める。#R479 まではモーダルの中だけで、両エンジンのレンダラ側 credit も消していた（`attributionControl:false` と `.cesium-widget-credits{display:none}`）ので、**地図を見ている読者には何も出ていなかった**。⚠ 名乗るのは**描かれている基図**——衛星の上に「© CARTO」を出すのは、省略より悪い偽の帰属である。⚠ capture mode でも消さない。⚠ **浮かせない**のは #R479 の実測が理由——右下の pill はレイヤーパネル（desktop 1280/1920）と下部ツールバー（携帯）に覆われ、座標読み取り・国情報・展開した Chronos・レイヤーパネルを全部立てた状態で 175×26 が収まる矩形は地図カラムに**1つも無かった**。どのオーバーレイの包含ブロックも届かない「行」なら、覆われようがない。 |
| **重い解析はクライアントで計算する**（DEM・物理・軌道・可視判定） | サーバーを持たない配信形態と、利用者の座標をサーバーへ送らない方針の両方に効く。 |
| **描いたものの出所と鮮度を必ず画面に出す** | 「取得できなかった」を「そこには何も無い」と描くのが、この地図で最も高くつく嘘である。未対応の国は灰色斜線、止まったフィードは年齢つきで印字する。 |
| **警報の「読んだ。何も出ていない」は、それが真である範囲にだけ描く** | 灰色は装飾ではなく主張である。国全体の灰色を、警報を描いている国の上に敷くとその主張は偽になる。どこで無いかを言えないズームでは、**言わない**（透明）ほうが正しい。 |
| **上流の粗い版へ落として速くしない**（気象モデルの領域） | Open-Meteo の `ecmwf_ifs025` は標本が 1/6.4 だが `valid_times` が3時間刻みで、既定の `ecmwf_ifs`（1時間刻み）と**軸が違う**。速さのために読み手が選んだ時刻をずらすのは、品質を落とさずに速くすることではない。速くするのは「同じ絵に着くまでの時間と通信」だけ。 |

## バックエンドと鍵

| 決定 | 理由 |
|---|---|
| **バックエンドは Supabase**（DB・認証・Edge Functions） | 認証・行レベル権限・サーバー実行を1つの契約で持てる。 |
| **API キーをフロントに置かない。AI はサーバー側の Edge Function が当方の鍵で実行する** | ブラウザに置いた鍵は公開したのと同じ。アカウント単位の1日上限も、鍵をサーバーに置いて初めて成り立つ。 |
| **公開 (anon) キーは公開前提とし、保護は RLS が行う** | 隠せない値を隠したことにする設計は、隠せなかった日に全部が破れる。 |
| **CARTO 基図のキーはフロントに置く（`js/carto-basemap.js` の `window.CARTO_BASEMAP_KEY`）** | 上の「API キーをフロントに置かない」の**例外ではなく、その下の行（公開 anon キー）と同じ扱い**である。静的サイトはタイル URL をそのまま読み手のブラウザへ渡すので、基図キーが秘密である配置は存在しない（CARTO は宣言したドメインに対して発行するが Referer は強制しない——実測で foreign referer でも透かしの無いタイルが返る）。守りは隠すことではなく、**綴りを1か所に閉じる**こと（回転が1行で済む）と、無料枠 5,000,000 タイル要求/月の中に居ること。 |
| **無認証の中継（5本）は共有ガードを通す** | allowlist・GET 限定・期限・バイト上限・外向きエラーは1語。1本ずつ書くと、1本だけがオープンプロキシになる。⚠ **allowlist はどれも接頭辞一致では書かない**——URL を解いてホストとパスを照合する（`startsWith` は「どこを指しているか」の検査ではない）。 |
| **ライブ航空機の位置は、サーバー（`aviation-feed`）が provider を1回読んで全利用者へ配る。ブラウザから上流を直接叩かない** | 上流の負荷が**利用者数に比例する**構造だったから断られた——ブラウザ1つが1掃引あたり最大 128 本の点問い合わせを出し、それが開いているタブの数だけ走っていて、airplanes.live は全リクエストに HTTP 403 を返すようになった。読み取りを**時間に比例**させる（TTL ごとに1回）なら、利用者が何人増えても上流から見える IntMap は変わらない。呼び出し側が選べるのはチャンネルだけで、相手先 URL は渡せない。 |
| **公開プロフィール (`profiles_public`) は view ではなく実テーブルで、トリガが `profiles` から同期する** | `security_invoker` を持たない view は**所有者の権限で基底表を読む**＝ RLS を迂回する。射影を4列に絞れば今日は漏れないが、**迂回は relation の性質であって射影の性質ではない**ので、明日足す1列が同じ迂回を継承する。`security_invoker = on` にすると `profiles` の own+admin ポリシーが効いて著者カードが消えるため、動かすには `USING (true)` ＋ **列単位の grant** に頼ることになる——それは #R155 が破られた壁（Supabase の既定権限がテーブルの ALL を配り直す）。**列を物理的に持たない表**なら、壁が grant でも policy でもなく構造になる。 |
| **ブラウザから直に読めない上流は、公開 CORS プロキシではなく自前の relay を先頭に置く**（株価＝`quotes-relay`） | 公開プロキシは、①**第三者に全要求が見える**、②**いつでも消える**、③**落ちても誰にも言わない**、の3つを同時に持つ。実測 2026-09-07、Companies タブの株価が持っていた私有の3段（直 Yahoo → corsproxy.io → api.allorigins.win）は**3段とも答えなかった**——直は 200 を返すが ACAO が無く、corsproxy は 403、allorigins は 522。つまり「段が3つある」ことは「経路が1つある」ことを意味しない。⚠ **同時に、その私有の梯子そのものが問題だった**——どの公開リレーを・どの順で・どこで見切るかという判断は `js/proxy-fetch.js` が既に持っており、2 つ目の写しは片方だけが直される。自前の relay を先頭に置き、後ろは**共有の梯子**に任せる。同じ判断はニュースと GDELT で先に下している（`## Atlas` の行）——**新しい方針ではなく、まだ従っていなかった最後の1か所である。** |
| **DB の変更は `supabase/migrations/` だけを通す。本番へ手で SQL を流さない** | 設計図が1つでないと、ローカルで通ったことが本番の保証にならない。 |
| **本番適用は1ファイルずつ（`db query --file … --linked` ＋ `migration repair`）** | ベースラインが本番に「適用済み」として記録されていないので、`db push` は生きた DB に対してベースライン全体を流そうとする。 |

## ニュース

| 決定 | 理由 |
|---|---|
| **保存する経路（`news_events`）の地点解析は AI が第一手段・決定論エンジンがフォールバック** | 決定論エンジンだけでは、出荷している記事の **48% が国単位**の粗さで止まる（本番実測 543/1,120）。AI は見出しの意味から都市・港・国境検問所まで降りられる。落ちたときに地図が空にならないよう、決定論エンジンを後段に残す＝**どちらか一方が死んでも地点は消えない**。 |
| **AI の確度は自己申告させず、決定論エンジンとの一致で測る** | 模型の自己申告は較正されていないのに、`location_confidence` として画面に出てしまう。独立に走る 2 つの仕組みが同じ場所を指したことは、後から人が確かめられる**本物の証拠**であり、食い違った行はそのまま運用者の待ち行列になる。 |
| **記事モード（検索・過去の日付・多言語）はブラウザ内の決定論エンジンのまま** | ネットワーク無し・乱数無しで、同じ見出しは常に同じ地点になる＝**検証できる**。そしてサーバーの鍵を要さないので、未ログインでもピンが出る。 |
| **ブラウザがニュース地点解析のために AI を呼ぶことは無い。「AIで解析」ボタンも作らない** | 見出しの地点は利用者に選ばせる種類の問いではないし、鍵の話でもある。 |
| **データの流れを変えたら、同じ変更の中でプライバシーポリシーも直す** | ポリシーは文章ではなく**コードについての事実の主張**である。`npm run check:docs` がその一致を検査する。 |

## 多言語

| 決定 | 理由 |
|---|---|
| **言語を1つ増やすコストはファイル1本**（`js/locales/ui.<code>.js`）。言語の集合＝ディレクトリの中身 | 人間が更新を覚えていなければならない一覧は、いつか間違っている一覧になる。 |
| **画面に出る文字はすべて9言語**。ゲートは `npm run check:i18n` | 「翻訳済み」の定義が複数あった間、どの計器も 100% を表示しながら英語が残っていた。 |
| **利用規約とプライバシーポリシーは日本語と英語だけ** | 法務文書の機械翻訳は**同じ文書ではない**。読んでいない版を提示するより、2言語であることを読者の言語で伝えるほうが正直である。 |

## Atlas

| 決定 | 理由 |
|---|---|
| **「何ができるか」の一覧は1つだけ持つ**（`js/atlas-capabilities.js`。ゲート＝`npm run check:capabilities`） | 以前は5つあった——dispatch の 115 case、SYS の 58 kB のカタログ、4 件しかない `ATLAS_ACTION_CAPABILITIES`、`IntMapOS` の登録済みコマンド、DOM を歩いて先頭 140 件を取る `controlCatalog`。**繰り返す欠陥はすべて、そのうち2つが食い違っていた**という形をしている。人が更新を覚えていなければならない一覧は、いずれ間違う。 |
| **`ok` を書けなくする**（`status === 'completed'` からの読み取り専用の導出） | 手書きの真偽値は、`catch(_){}` の直後でも書ける。書ける限り、いつか書かれる。 |
| **実行できる操作は必ずカタログに載せる**（ゲート＝`npm run check:catalog`） | カタログに書かれていない操作は**プランナーにとって存在しない**。実装済みの6機能が、この検査が無い間ずっと「その機能はありません」と答えられていた。 |
| **成功を名乗る前に効果を検証する** | 「実行しました」とだけ言えるモデルは、何もしなくても同じ文を書ける。 |
| **対象が要る操作は、地図の中心で代用せず訊く** | 中心は「利用者が指した場所」ではなく「たまたま画面の真ん中にあった場所」。代用すると、間違った答えが正しい形で返る。 |
| **カタログは押し付けず、訊かれたときに返す**（中核数ツール ＋ `find_capability` がレジストリの全131を検索） | 全能力の説明文（64,250文字）を毎ターン送るのをやめ、「関連分だけに絞る」もやめた。**絞り込みは文を読んでいなかった**——実測で「ありがとう」も「東京の天気は？」も**同一の26件・41,178文字**で、選別を決めていたのは `produces:'explanation'` の加点だった。送る量は減ったが、**到達できる範囲はレジストリ全部のまま**（130 のうち撤去済み 1 を除く 130）。 |
| **地図を使うかは Atlas が宣言し、宣言したことをコードが守らせる**（`answer_mode` × `changedMap`。旧「地名が出たら地図化」は戻さない） | #R406 が撤去した義務は正しく撤去された——地名は地図化の理由ではない。しかし撤去の跡に「地図が回答を担うと決めた回答を、実際に地図回答として完成させる仕組み」が無く、実測では地図を使うと自分で決めた回答が文だけで終わっていた。言葉から推定する規則を戻す代わりに、**Atlas 自身の宣言**を読み、宣言と結果が食い違う final だけを返す——引数が schema に合わない呼び出しを返すのと同じ種類の検査。差し戻しは上限つきで、上限後はそのまま受け入れて記録する。 |
| **「地図で説明する」は 1 回の呼び出しにする**（`map.compose`。小さな道具の組み合わせに任せない） | 地点・役割・関係・塗り・カメラ・凡例を 6 つの道具で 6 手に分けると、6 回の判断になる——実測ではモデルはその 1 つも選ばず、文を書いて終えた。合成層はモデルに「何を説明したいか」だけを言わせ、描画への変換をコードが引き受ける。座標は変わらずモデルに書かせない（台帳 → ジオコーダ）。 |
| **意味を決めるのは Atlas、形を決めるのはコード** | コードが見るのは**ツールの実在・引数の型・必須引数・回数の上限・安全**だけ。「質問か」「地図が要るか」「今の話か歴史か」は Atlas が決める。正規表現は ISO 日付・座標のような**決定的な構文解析にだけ**使う。理由: 68 か所の意味判定は、どれも実在の不具合への対処として足され、**互いに矛盾しながら蓄積した**。新しい失敗に正規表現を足したくなったら、それは意味判定がまだコード側に残っている兆候。 |
| **能力は自分の引数を宣言する**（131本の schema・ゲート＝`check:capabilities` の ㉑㉒） | 以前は126全部が `{type:'object'}` を共有しており、何を渡しても通った。引数ゼロの `analyze` が実行段階まで到達し、「何を分析しますか？」になっていた。⚠ **必須引数の強制はツール面で行い、kernel へは型だけを渡す**——ボタンが引数なしで押して `resolveInputs` が訊き返すのは正しい振る舞いだから。 |
| **AI 利用枠の単位は「1リクエスト」ではなく「1 user turn」** | 1つの質問が planner ＋ 最大2回の修復で終わるのに、利用者は3回分を無言で払っていた。ターン鍵はサーバーがアカウントに紐づけ、回数と寿命を上限つきで管理する（**クライアントの鍵は信用しない**）。 |
| **費用の桁が違う操作には別の枠を与える。既存の枠を広げない**（用語グロス＝`ai_gloss_usage`。free 60/日） | 回答文の語句を引く操作は prompt が短く、出力 700 token で、ツールも web 検索も使わない。それを質問と同じ 10 回の枠に載せると、**1 つの回答を読む間に 3 語調べただけで質問が残らない**——機能が「使えるかどうか」ではなく「目的が成り立つかどうか」で失敗する。枠を **20 に上げる**のは同じ問題を先送りするだけで、質問とグロスが互いを塞ぐ構造は変わらない。 |
| **どちらの枠で払うかはヘッダで宣言し、本文を読んでから照合する**（`x-intmap-lane` × `task`。不一致は払い戻して 400） | 消費は本文を parse する**前**に起きる（枠切れの呼び出しの本文を読まないため）ので、レーンは body に置けない。置ける場所がヘッダしか無い以上、それは**まだ誰も読んでいない本文についての自己申告**であり、照合しなければ「安いカウンタで高い task を買う」ドアになる。 |
| **schema は provider にも届ける**（方言を変換し、拒否されたら `json_object` へ落ちる梯子にする） | 前の判断は「方言が違うので今回は入れない／schema 自体は proxy まで届いており、方言が揃った provider ではそのまま効く」だった。**その「そのまま効く」が成り立っていなかった**——`callOpenAI` に schema 引数が存在せず、`ANSWER_SCHEMA`・`MAP_REPORT_SCHEMA`・`PLAN_SCHEMA`・`RESEARCH_MAP_SCHEMA`・`GEO_RESOLVE_SCHEMA` は**現行 provider に一度も届いていなかった**（届いていたのは `{type:"json_object"}` だけ）。フィールド名・列挙・必須集合が、タスク規則の散文とクライアントの事後正規化にしか存在しない状態だった。⇒ 方言変換（大文字型名→小文字・任意フィールドは `null` へ広げる・列挙も同時に広げる）を入れ、**400 は既存の梯子で `json_object` へ落ちる**ので、方言を嫌うモデルでも失う応答は無い。**クライアント側の決定論的検証は変えていない**——梯子が schema を落とした回もあるので、両方が要る。 |
| **「質問の中心命題に答えたか」は警告に留める**（`error` にしない） | 実測: `error` として書いたら **`tests/r350` の正しい回答例で発火した**。質問は「中華人民共和国は世界有数の経済規模。実際に支えているのは何？」、回答は「需要面では最終消費が最大で、規模そのものを可能にしているのは製造能力・供給網…」——**良い回答が質問の名詞を1つも再利用していない**。語の重なりで測る限りこの例は通せないので、`error` にすると正しい回答が修復パスへ送られ、利用者の1日分の枠を余分に使い、悪くなる可能性がある。**所見は記録し、修復プロンプトからは読める**。`error` へ上げるには主語・焦点を解析する仕組みが要る（ストップワードを増やすことではない）。 |
| **質問の意図は capability カタログより強い**（`_validatePlan` で決める） | カタログに `brief` があることは、利用者が報告書を望んだ理由にならない。「地図製品だから」も視点を動かす理由にならない。**プロンプトの注意書きではなく validator で**——60 kB の system prompt の 1 文は要請であって決定ではない。明示要求の無い `brief` は `analyze` へ、地図意図の無い視点・選択操作は落とす。⚠ ただし**消さずに差し替える**（質問には答えが返らなければならない）。⚠ そして**旅行・旅程は地図意図**（「台湾へ旅行したい」には地図の動詞が1つも無い）。 |
| **座標・URL・出典をモデルに生成させない** | 生成できてしまうものは、生成されていないことを確認できない。IntMap が集めた証拠に ID を付け、モデルにはその ID だけを返させる。 |
| **ターンは必ず終わる。終わらせるのは回数ではなく時計である** | 回数の上限（手数・呼び出し数）は**時間について何も言っていない**。実測: 1 回の証拠取得が公開 relay 3 本を 9 秒ずつ逐次に歩き、`analyze` がそれを半ダース走らせ、1 手に道具が 8 本並び、手数は 8——掛け算の結果は数字ではなく「ずっと Searching」だった。⚠ **これは Atlas の裁量を削るものではない**（CONSTITUTION.md §5）: 出所も道具も件数も 1 つも減らしていない。変わったのは、締切に間に合わなかった取得が**「取得不可」として名指しで報告される**（この pipeline が常に読み手に見せてきた `missing` の一覧）ことと、道具が返らなかったことが `tool_timeout` として **Atlas に伝わる**ことだけ。**何も答えないターンは、読み手に何も伝えていない。** |
| **締切はヘッダではなく本文の読み終わりまで掛ける** | `clearTimeout` を `fetch` の完了で呼ぶ書き方は「締切がある」ように読めるが、止められるのはヘッダが着くまでだけで、**200 を返してから本文が止まった相手には何も効かない**。実測（2026-08-25・本番オリジン）: `api.allorigins.win` はヘッダを **8.7 秒**で返す——8 秒の締切のすぐ外側。 |
| **停止と「次の質問」は、モデル呼び出しだけでなく取得にも届く** | ターンごとの `AbortController` は作られていたのに、**最も時間のかかる証拠取得だけがそれを見ていなかった**。だから停止は吹き出しの見た目を変えるだけで、質問を送り直すことは前の試行を**置き換えず、足していた**。未完了の梯子が読み手の再試行のたびに 1 組ずつ増える状態は、上限が無いのと同じである。 |
| **Google News RSS も GDELT も、自前の Edge Function を先頭に置く**（`news-relay` / `gdelt-relay`） | 塞がり方は違うが、答えは同じになった。news.google.com は ACAO を返さないので**構造的に**読めない（#R216）。GDELT は答えるときは ACAO を返すので直に読める——**が、答えることがめったに無い**。実測 2026-08-25、本番オリジンから直に 18 回で**成功 4 回 (22.2%)・成功時の中央値 17.5 秒**、拒否は ACAO の無い 429 で**中央値 12.3 秒**。公開 relay は 4 本とも死んでいた（corsproxy 403・codetabs 522・allorigins timeout・corsfix 403）。⚠ **かつてここには「GDELT を自前 relay に寄せると、全読者が 1 つの egress IP を共有するので悪化する」と書いてあった。その予測を測って、外れた**——同じ分・同じクエリで住宅 IP（JP）と Supabase edge（ap-northeast-1）は**見分けがつかない**（429 が 8 割・10.7〜26.0 秒）。GDELT は文書化された「5 秒に 1 回」よりはるかに手前で絞っており、**10 秒空けて送った要求も 429 だった**＝守るべき「読者ごとの枠」が存在しない。⚠ **上流の負荷を実際に下げるのは、ブラウザにできないこと＝答えを憶えること。** GDELT 自身が `cache-control: public, max-age=900` を返すので、`gdelt-relay` はクエリ単位で 15 分共有し、6 時間までは古い答えを返しながら裏で更新する。**要求は読者数ではなく時間に比例する**（#R341 の aviation-feed と同じ構造）ので、直に叩いていた頃より要求は減る。キャッシュがある場合の実測 **0.6 秒**。公開 relay は**その後ろ**に、競争させたまま残す。 |

## 品質のしくみ

| 決定 | 理由 |
|---|---|
| **規則を文章で書いたら、その規則を測る検査を同じ変更の中で書く** | 書いただけの規則は守られない。この文書の項目のほとんどは、**書いてあったのに誰も突き合わせていなかった**ものが実際に嘘になってから検査を得ている。 |
| **テスト時間には天井があり、下げる方向にしか動かさない** | 試験を足すたびに待ち時間が伸びる設計は、いつか「今回は流さない」で終わる。 |
| **文書は事実ごとに正本を1つに決め、`npm run check:docs` が実装と突き合わせる** | 同じ事実を2か所に書くと、片方だけが古くなる。どちらが古いかは、読んだ人には分からない。 |

## 交通情報つきルーティングの provider は、機能ではなく規約で決まった

「リアルタイム交通量を反映する」には外部 provider が要る。候補を**条項の実物**で判定した結果、
IntMap の構成（**MapLibre + OpenStreetMap のベースマップ**・静的配信・現在地追従の逐次案内）で
使えるものは1つしか残らなかった。

| provider | 判定 | 決め手 |
|---|---|---|
| Google Routes | **不可** | maps-service-terms §19.2「must not use Google Maps Content from the Routes API in conjunction with a **non-Google map**」／一般規約 §3.2.3(e) は "with **or near**" なので、地図の隣に文字だけ出す回避も塞がれている |
| Amazon Location | **不可** | AWS Service Terms §82.5.b が「HERE のルートを他社地図に重ねること」を名指しで禁止。加えて日本は routing / traffic とも非対応 |
| HERE | **不可（この用途では）** | Platform Terms 2.1 が **Navigation Functionality**（現在地に基づく逐次案内）を標準ライセンスから除外し別途書面契約を要求。6.4(a) 末文が日本の Content の第三者コンテンツ併用を PoI / visual / dynamic に限定。日本では `pedestrian` / `bicycle` 自体が非対応 |
| TomTom | **不可（この用途では）** | Portal T&C 2.1 が同じく Navigation Functionality を除外。日本に対応するのは Orbis v2 のみで、**Orbis にはターンバイターンもレーンも無い** |
| GraphHopper | 規約はクリア・**交通が無い** | ToS 第5章「with and without showing a map」で明確に可。だが OpenAPI spec に `/route` の traffic パラメータが 0 件（「not yet available for openstreetmap」） |
| **Mapbox** | **採用** | 「Mapbox Map 上に限る」条項は Boundaries / POI / Studio の3つで **Navigation API を含まない**。attribution 文書が「on a non-Mapbox map」を明示的に想定。日本の交通データあり |

**その規約はコメントではなくフィールドとして符号化してある**（`js/routing-providers.js`）。
コメントは編集を止めないため:

- `noStore: true` — Product Terms §2.10.1 が結果の cache / store を禁じる。⇒ `routing-relay` **だけ**が
  `Cache-Control: no-store` を返し（他の relay は `s-maxage`）、アダプタは storage API を1つも使わない。
- `aiContent: false` — §1.5(ii) が AI への利用を禁じる。Atlas はこのアプリの操作卓なので
  「経路をモデルに渡す」が既定の動作になりうる。**この provider の返り値についてはそれをしない。**
- `evidence: 'documented'` — 鍵がリポジトリに無いので、これらのフラグは**1つも観測していない**。
  `available()` は relay が答えるまで false ＝ **文書の力だけで利用者に機能を提示しない**。

⚠ **鍵が入るまで、交通に関するものは1つも UI に出ない。** 出ないことが正しい状態であって、
「準備中」のボタンを置くことではない。

## 企業アトラスは、リンクが明示されている施設だけを載せる

**判断**: 企業の拠点は、上流が**その企業との関係を明示している**ものだけを載せる。名前が一致する
だけの施設は載せない。OSM の `operator:wikidata` / `owner:wikidata` は載せ、`brand:wikidata` は
**小売の存在**として別扱いにする。

**理由**: 「Toyota ...」という名前の工場を Toyota のものと見なすのは推論であって出典ではない。
実測: ドイツの `brand:wikidata=Q53268` は 1 件残らず独立資本のディーラーで、多くは自前の
`operator` を持つ。これを施設として出せば、**Toyota が持っていない事業所 5,262 件**を主張すること
になっていた。同じ理由で所有関係の推移閉包も使わない——Toyota から JR 東海に届き、東海道本線の駅が
全部「拠点」になった（実測）。**一段＋型ゲート**だけを辿る。

**代償**: TSMC の台湾の fab のように、どの公開データにも企業との明示リンクが無い施設は載らない。
それは欠損として `npm run check:companies --report` に出る。**埋めない。**
正本 [`docs/COMPANIES.md`](docs/COMPANIES.md)。

### ロゴも同じ規則の下にある——ビルド時に解決して同梱し、閲覧時に第三者へ訊かない

**判断**: 企業ロゴは、拠点や財務と同じ「出典のある事実」として**ビルド時に**解決する
（Wikidata P154 → Wikimedia Commons）。閲覧時にドメイン名から画像 URL を組み立てる第三者 API は
使わない。ロゴが無い企業は**モノグラム**で、要求そのものを出さない。

**理由**: 3 つあり、どれか 1 つでも成り立てば十分だった。

1. **閲覧が漏れる。** ドメインを送ってロゴを取る API は、送るたびに「いま誰を見ているか」を
   第三者に渡す。地図・拠点・財務は 1 バイトも外へ出ないのに、ロゴだけがそれを配っていた。
2. **上流が消えても壊れない。** 第三者のロゴ API は、実際に消えた（Clearbit Logo API は
   2025-12-08 に停止し、`logo.clearbit.com` は DNS からも消えた。8.8.8.8 / 1.1.1.1 / 9.9.9.9 の
   どれも A も CNAME も返さず、権威 SOA だけを返す）。同梱した画像参照には、その壊れ方が無い。
3. ⚠ **正しい答えは、すでにこのリポジトリの中にあった。** 詳細パネルは最初から Commons の
   ロゴを描いていて、死んだホストを見ていたのは**一覧だけ**だった——索引に載っていなかったから。
   **「別のロゴ提供者を探す」は、この問題の解ではない。**

**代償**: Wikidata が P154 を持たない企業（533 社中 98 社）は Commons のロゴを持たない。
そこだけは Google の favicon にドメイン名を送り、それも無ければモノグラムになる。
**架空のロゴは出さない**——空欄が拠点や財務で「欠損」であるのと同じ扱いである。
正本 [`docs/COMPANIES.md`](docs/COMPANIES.md) §4.3。
